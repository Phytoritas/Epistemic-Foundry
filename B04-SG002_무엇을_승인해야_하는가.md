# B04-SG002는 무엇이고, 무엇을 승인해야 하는가

## 결론부터 말하면

`B04-0006`에서 **126개 schema와 OpenAPI 1개를 package snapshot으로 옮기는 작업은 성공했습니다.** wheel과 sdist도 정상적으로 만들어졌고, 설치된 wheel만으로 127개 canonical resource를 읽는 검사도 통과했습니다.

그런데 전체 회귀 검사를 실행하자, 새 canonical 계약을 아직 따르지 않는 runtime 파일 2개와 F04/J02/S04의 파생 자료가 발견됐습니다. 특히 runtime 파일 2개는 현재 `development_manifest`에서 **수정 담당자가 지정되어 있지 않습니다.** 담당자와 C04 이전 실행 순서를 임의로 정할 수 없으므로 `B04-SG002`로 중단한 상태입니다.

즉, 지금 필요한 승인은 “projection을 다시 할 것인가”가 아닙니다. projection은 이미 성공했습니다. 필요한 것은 다음입니다.

> 새 canonical 계약에 맞게 runtime 2개와 F04/J02/S04 파생 자료를 누가, 어떤 exact path 안에서, 어떤 순서로 교정할지 승인하는 것

---

## 무엇이 성공했는가

`B04-0006`의 canonical projection 결과는 다음과 같습니다.

| 항목 | 결과 |
|---|---:|
| root canonical schemas | 126 |
| packaged schemas | 126 |
| OpenAPI resources | 1 |
| total canonical resources | 127 |
| missing paths | 0 |
| extra paths | 0 |
| hash mismatches | 0 |
| duplicate schema `$id` | 0 |
| targeted projection tests | 41 passed |
| clean wheel/sdist build | PASS |
| sdist → wheel rebuild | PASS |
| installed-wheel-only load | PASS |
| arbitrary empty cwd load | PASS |
| source-tree fallback success | 0 |
| deterministic rebuild | PASS |

봉인된 주요 해시는 다음과 같습니다.

- source bundle: `sha256:1557b03db2ad7e7d23b014d4c9d5fd643803f6613696c966d9b0379573259e7f`
- package snapshot: `sha256:d01bda0057584e235331b649238fc2507c60cab329fd6b8e8b6a115fac912559`
- canonical registry: `sha256:6b4fcade707639e537744be4075e71d3f7e068cd42eaaaddb20ef084851175d5`

따라서 root source와 package snapshot 사이의 124→126 migration은 완료됐습니다. 이 성공을 취소하거나 다시 실패로 바꿀 이유는 없습니다.

---

## 무엇이 실패했는가

projection 뒤 전체 Python과 Node 회귀 검사는 green이 아니었습니다.

### Python

- collected: 983
- passed: 916
- failed: 52
- errors: 15
- skipped: 0
- 총 문제: 67

분류는 다음과 같습니다.

| 분류 | 수 | 원인 |
|---|---:|---|
| `GATE_DECISION_RUNTIME_SCHEMA_DRIFT` | 51 | runtime이 새 `GateDecision` 필수 필드를 만들지 않음 |
| `HOLDOUT_MANIFEST_RUNTIME_SCHEMA_DRIFT` | 15 | runtime이 새 `HoldoutManifest` 구조를 만들지 않음 |
| `J02_TIKTOKEN_DEPENDENCY_DEBT` | 1 | 기존 J02 dependency debt |

### Node

Node TAP의 authoritative total은 다음과 같습니다.

- tests: 458
- passed: 447
- failed: 11
- skipped: 0

JUnit에는 실제 failing testcase 10개가 기록되어 있습니다. TAP의 11 failures와 1개 차이가 나는 이유는 실패한 nested suite/container 집계가 하나 더 포함되기 때문입니다.

| 소유 영역 | failing testcase | 원인 |
|---|---:|---|
| F04 | 7 | golden-flow fixture가 새 `GateDecision` 필수 필드를 만들지 않음 |
| J02 | 2 | sealed skill inventory의 authority source hash가 stale |
| S04 | 1 | active development manifest와 source binding successor hash가 불일치 |

---

## 왜 내가 임의로 고치지 않았는가

두 핵심 runtime 경로에는 현재 manifest owner가 없습니다.

- `src/epistemic_foundry/foundry_kernel/gates.py`
- `src/epistemic_foundry/verifier_firewall/firewall.py`

이 파일들은 각각 canonical `GateDecision`과 `HoldoutManifest`를 생성하는 코드이므로 반드시 고쳐야 합니다. 하지만 어느 work package가 해당 변경과 관련 테스트를 소유하는지 상위 계약에 없습니다.

나머지 파일은 owner가 있지만, C04 전에 re-entrant correction으로 다시 실행할 권한과 순서가 없습니다.

- F04 owner: `tests/golden/forge/**`
- J02 owner: `plugins/epistemic-foundry/skills/skill-inventory.json`
- S04 owner: `manifests/source_bindings/development-manifest.binding.json`

C04는 전체 suite 0 failures를 요구합니다. 따라서 위 교정을 하지 않은 채 C04를 시작할 수 없습니다. 반대로 현재 계약 없이 C03/F04/J02/S04의 실행 순서나 write scope를 임의로 확대하는 것도 금지됩니다.

이 권한·순서 공백이 `B04-SG002`입니다.

---

## 사용자가 승인해야 하는 최소 내용

### 1. runtime correction owner

권장 owner는 기존 runtime migration package인 `C03`입니다. 새 attempt는 `C03-0003`을 권장합니다.

C03의 기존 scope는 유지하고 다음 exact path만 추가 승인하면 됩니다.

- `src/epistemic_foundry/foundry_kernel/gates.py`
- `src/epistemic_foundry/verifier_firewall/firewall.py`
- `tests/test_evidence_parliament.py`
- `tests/test_foundry_kernel.py`
- `tests/test_product_invariants.py`
- `tests/test_verifier_firewall.py`

다음 두 테스트는 이미 C03 scope에 있으므로 중복 추가할 필요가 없습니다.

- `tests/test_governance.py`
- `tests/test_integration_forge_cycle.py`

이 correction은 schema를 약화하는 작업이 아닙니다. runtime이 이미 승인된 canonical schema를 정확히 생성하도록 맞추는 작업입니다.

### 2. F04 correction을 C04 전에 실행할 권한

- 권장 attempt: `F04-0002`
- 수정 대상: 기존 F04 scope인 `tests/golden/forge/**` 안의 fixture
- 목적: golden flow가 완전한 canonical `GateDecision`을 생성하도록 교정

### 3. J02 correction을 C04 전에 실행할 권한

- 권장 attempt: `J02-0003`
- 수정 대상: 기존 J02 scope의 sealed inventory와 관련 J02 fixtures/evidence
- 목적: 실제 authority source bytes에 맞춰 content hash를 결정론적으로 재생성
- 금지: 검증을 끄거나 hash mismatch를 무시하는 fallback

### 4. S04 correction을 C04 전에 실행할 권한

- 권장 attempt: `S04-0003`
- 수정 대상: 기존 S04 exact scope
- 목적: 현재 development manifest용 새 source-binding revision, patch plan, authorizing decision linkage와 self-hash 생성
- 금지: active manifest hash를 검사에서 제거하거나 hard-coded allowlist로 우회

### 5. 고정 실행 순서

권장 순서는 다음과 같습니다.

```text
C03-0003 runtime correction
  ↓
F04-0002 fixture correction
  ↓
J02-0003 sealed inventory correction
  ↓
S04-0003 active manifest binding correction
  ↓
B04-0007 pre-C04 projection/regression revalidation
  ↓
C04-0002 full conformance gate
  ↓
B04-0008 final packaging gate
  ↓
전체 dependency DAG 재계산
```

이 순서는 영구 정적 dependency cycle을 추가하는 것이 아니라, 이번 shared-contract migration을 닫기 위한 attempt-level correction sequence입니다.

### 6. 이력 보존

다음 두 사실을 동시에 보존해야 합니다.

1. `B04-0006`의 canonical projection은 검증된 성공입니다.
2. `B04-0006` package 전체 상태는 `B04-SG002` 때문에 `SPEC_GAP`입니다.

기존 attempt, report, review, RAH evidence, generation 및 dirty working tree를 삭제하거나 소급 변경하지 않습니다.

---

## 승인하지 않아도 되는 것

다음과 같은 광범위하거나 위험한 권한은 필요하지 않습니다.

- broad `src/**` 또는 `tests/**` 수정
- canonical schema 완화
- missing required field를 optional로 변경
- skip/xfail 또는 failure allowlist
- stale hash를 무시하는 runtime fallback
- source-tree/editable-install fallback
- 기존 report나 SPEC_GAP 삭제
- C04 또는 final B04 조기 시작
- `completion_ready=true` 조기 전환

---

## 그대로 검토·승인할 수 있는 HumanDecision 초안

아래 내용은 권장 최소 승인안입니다. 이 파일에 초안이 존재하는 것 자체는 제품소유자 승인 기록이 아닙니다. 사용자가 명시적으로 승인한 뒤 durable HumanDecision으로 기록해야 효력이 생깁니다.

```text
# AUTHORITATIVE PRODUCT-OWNER SHARED-CONTRACT DECISION
# Epistemic Foundry v4.0 — B04-SG002
# Decision ID: HD-EF4-B04-SG002-20260730-001

이 메시지는 사용자이자 제품 최상위 권한자의 명시적
shared-contract HumanDecision이다.

B04-0006의 deterministic canonical projection은 VERIFIED/PASS다.
B04-0006 package가 B04-SG002에서 fail-closed 중단한 판정도 정당하다.
B04-0006을 소급하여 전체 PASS로 변경하지 않는다.
기존 attempt, report, review, commands, projection receipt, RAH evidence,
generation 및 dirty working tree를 모두 보존한다.

1. runtime correction owner

C03을 GateDecision 및 HoldoutManifest runtime migration의 owner로 지정한다.
다음 attempt는 C03-0003으로 실행한다.

C03의 기존 write scope를 보존하면서 다음 exact path만 추가한다.

- src/epistemic_foundry/foundry_kernel/gates.py
- src/epistemic_foundry/verifier_firewall/firewall.py
- tests/test_evidence_parliament.py
- tests/test_foundry_kernel.py
- tests/test_product_invariants.py
- tests/test_verifier_firewall.py

tests/test_governance.py와 tests/test_integration_forge_cycle.py는
기존 C03 scope를 그대로 사용한다.

C03-0003은 canonical GateDecision과 HoldoutManifest를 완전히 생성하고,
schema weakening, default fabrication, placeholder hash 또는 silent fallback
없이 관련 Python 회귀를 해결한다.

2. pre-C04 re-entrant corrections

C03-0003 PASS 후 다음 correction attempts를 C04 전에 직렬 실행한다.

- F04-0002:
  기존 tests/golden/forge/** scope 안에서 canonical GateDecision fixture 교정
- J02-0003:
  기존 J02 scope 안에서 sealed inventory와 authority source hash 재생성
- S04-0003:
  기존 S04 scope 안에서 current development manifest용 새 binding revision,
  patch plan, authorizing decision linkage와 binding self-hash 생성

각 correction은 기존 package history를 수정하지 않고 새 attempt로 기록한다.

3. fixed resume sequence

다음 순서로만 재개한다.

1. HD-EF4-B04-SG002-20260730-001 durable 기록
2. B04-SG002 resolved_by 관계 append
3. development manifest의 C03 exact write-scope/required-check correction
4. C03-0003 runtime correction 및 PASS 확인
5. F04-0002 correction 및 PASS 확인
6. J02-0003 correction 및 PASS 확인
7. S04-0003 correction 및 PASS 확인
8. B04-0007 pre-C04 projection/regression revalidation
9. Python/Node 신규 failure 0과 matching projection receipt 확인
10. C04-0002 full conformance gate
11. C04 PASS 확인
12. B04-0008 final packaging gate
13. B04 final PASS 확인
14. 전체 dependency DAG 재계산
15. 다음 dependency-ready package 재개

어느 단계든 PASS하지 않으면 후속 단계를 시작하지 않는다.

4. preservation

- B04-0006 projection_status: PASS
- B04-0006 package_status: SPEC_GAP
- blocker: B04-SG002
- completion_ready: false

위 상태와 기존 RAH/report/review/commands/projection evidence를 보존한다.

5. prohibitions

- schema weakening
- required canonical fields의 optional/default 완화
- skip/xfail 또는 allowlist로 failure 은폐
- stale hash 또는 stale snapshot fallback
- broad src/**, tests/**, manifests/** 권한 확대
- 기존 attempt/report/evidence 삭제 또는 소급 PASS
- C04 전 correction 생략
- C04 PASS 전 final B04 시작
- receipt 없는 completion
- reset, clean, stash 또는 dirty working tree 훼손
- completion_ready 조기 true 전환

이 결정이 durable하게 기록된 뒤 B04-SG002는
RESOLVED_BY_DECISION이 되며, C03-0003부터 고정 순서로 재개한다.
```

---

## 현재 정확한 상태

- `C01-0006`: PASS
- `C02-0002`: PASS
- `C03-0002`: PASS
- `B04-0006` projection: PASS
- `B04-0006` package: SPEC_GAP (`B04-SG002`)
- `C04-0002`: 시작하지 않음
- final B04: 시작하지 않음
- RAH: blocker 봉인 대상
- `completion_ready`: `false`

사용자 승인이 durable하게 기록되기 전에는 C04나 final packaging을 진행하면 안 됩니다.
