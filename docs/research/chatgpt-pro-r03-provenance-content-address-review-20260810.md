NO_BLOCKER — The repair restores builder–validator parity: observation membership, conflict identity and semantics, canonical explanations, kind counts, status, `aporia_id`, and `aporia_hash` are re-derived from published fields. Any coherent content change now becomes a different content-addressed record rather than laundering under the original identity; adding sorted `observation_ids` closes no-conflict ID collisions. With no shared AporiaRecord schema or persisted consumer in the stated repository state, the intentional ID-preimage change creates no material compatibility or migration blocker.
