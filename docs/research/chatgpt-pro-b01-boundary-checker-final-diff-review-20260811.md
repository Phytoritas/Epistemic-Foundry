B01_CHECKER: BLOCKED — `const loaded = import(("@epistemic-foundry/contracts/src/private.mjs"));` is valid executable JavaScript, but `moduleSpecifierTokens()` handles `import(` by passing only `tokens[index + 2]` to `add()`; here that token is the inner `(`, so the private literal is never checked. Replace the fixed-offset argument lookup with a local helper that unwraps balanced transparent parentheses only when they contain exactly one string/template literal, and use it for `import(...)`, `require(...)`, and `require?.(...)`; computed expressions must remain unextracted. 
