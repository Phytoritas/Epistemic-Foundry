NO_BLOCKER — `_resolve_child_id` restores the declared replay rule: only `None` invokes `new_id`, while any supplied invalid identifier fails closed. Strict `_require_text` validation likewise prevents integer, boolean, or byte-like values from aliasing canonical open-question IDs while preserving valid string citations, duplicate collapse, and deterministic sorting. This matches R05’s explicit requirements that caller-supplied identifiers make applications re-derivable and that Aporia-grounded operators cite questions the validated graph actually leaves open.  
