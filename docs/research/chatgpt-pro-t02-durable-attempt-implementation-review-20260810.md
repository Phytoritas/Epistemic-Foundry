NO_BLOCKER — The bounded implementation closes the crash-order and concurrent-replay defect: only the first linearizable CAS transition that durably creates and binds the eight-field Attempt may grant execution, while every pre-existing Attempt fences redispatch as reconciling. This matches E02’s private Attempt model and its required distinction between `NOT_STARTED` without an Attempt and non-retryable `RECONCILING` once an Attempt exists.   

Attempt-indexed receipt lookup closes the receipt-persisted/bind-missing crash window, and append-only `UNKNOWN → terminal successor` handling with predecessor proof preserves reconciliation history without permitting a second effect. The T02-local `EFFECT_RECONCILING` detail does not change the shared T01 envelope or canonical receipt shape. The remaining boundary is stated truthfully: this proves at-most-once invocation relative to a correct linearizable lifecycle port, not provider-side exactly-once execution or a live Python-to-E02 kernel binding.
