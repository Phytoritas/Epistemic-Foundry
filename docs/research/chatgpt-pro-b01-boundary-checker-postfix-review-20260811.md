B01_CHECKER: BLOCKED — `const privateModule = (require)("../../contracts/src/private.cjs");` is valid executable CommonJS, but at the `require` token `callOpenIndex(index + 1)` sees the transparent group’s closing `)` rather than the subsequent call-opening `(`, returns `null`, and the private literal is never extracted. Smallest one-file repair: add a local helper that recognizes an otherwise-empty balanced parenthesized wrapper around the direct `require` callee, then feeds the following call parenthesis to the existing `literalCallArgument()`; do not unwrap member or computed callees. 
