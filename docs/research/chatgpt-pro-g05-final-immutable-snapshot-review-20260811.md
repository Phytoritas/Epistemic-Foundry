**BLOCKER:** `surface.mjs:299–303` and `loadSurface()` at `525–530, 599–613` read each `agents/openai.yaml` through a fresh filesystem call, outside the private `sourceBytes` snapshot. Those agent-card values materially control routing through `skillCandidate()` at `645–657`—including `sensitive`, `side_effecting`, trigger phrases, and exclusion phrases—but `sourceDigests` covers only the five static `RECEIPT_SOURCE_PATHS`. Changing an exclusion phrase or an unprojected policy flag can therefore change routing behavior while preserving the same `surfaceReceipt().receipt_hash`.

**SMALLEST G05-LOCAL CORRECTION:** After parsing `surface` from its captured bytes, derive the sorted agent-card paths for its declared skills, read each path exactly once into the same private `sourceBytes` map, parse every card through `sourceText(path)`, and include those paths and captured-byte digests in `sourceDigests`. Preserve the current `deepFreeze`, read-only Map facades, surface-membership guard, and inventory-integrity logic unchanged.
