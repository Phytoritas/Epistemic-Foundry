NO_BLOCKER — The prior schema-overconstraint is closed: source `unsearched_scopes` now preserves all schema-valid string inputs while only the synthesis projection is canonicalized, and every nonempty source sequence still degrades status. The `abs_tol=1e-9` reconciliation is compatible with seven independently 10-decimal-rounded direction totals—the maximum rounding-only discrepancy is (4\times10^{-10})—while raw counts remain exact, so neither delta permits a material status or independence overclaim. This remains within R01’s independence-adjusted, null/moderator-retaining synthesis boundary. 
