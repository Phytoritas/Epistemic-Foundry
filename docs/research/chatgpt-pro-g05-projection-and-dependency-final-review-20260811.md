**G05_LOCAL: PASS.** The bounded G05 diff now places every canonical projection-row field—`command`, `mutating`, `segments`, `title`, and `tool`—inside the receipt preimage, and the same-count projection-drift regression changes an otherwise unclaimed row and requires a different receipt hash. `loadSurface()` also correctly retains `AUTHORITY_PREDICATE_EMPTY`, so the current 13-row dependency fails closed rather than weakening the authority exclusion.  

**SHARED_DEPENDENCY: SPEC_GAP.** The current T03-owned `commandSurface()` is explicitly built only from T01’s read/planning `toolDescriptors()`. Higher authority defines T01 as the exact 13-tool read/planning surface, defines T02 separately as the mutating-tool package, and declares T03 dependent only on T01; G05’s declared dependencies likewise do not include T03. At the same time, the G05 contract requires the composed 22-row surface and a non-empty mutating authority-bearing subset. Thus no frozen dependency or ownership edge currently authorizes T03—or another named owner—to compose T01 and T02 for G05. Restoring 22 rows would otherwise introduce an undeclared T03→T02 dependency, so this is not merely stale implementation drift.    

The smallest owner-level resolution is to amend `MASTER_SPEC.md` and `development_manifest.yaml` to declare that T03’s public `commandSurface()` is the exact deterministic union of T01’s 13 read/planning descriptors and T02’s 9 mutating descriptors, add T02 as a T03 dependency, and add T03 as a G05 dependency. The T03 owner can then restore the composed 22-command projection; G05’s receipt binding and non-vacuity gate remain unchanged.
