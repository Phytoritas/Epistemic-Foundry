AUTHORIZED_LOCAL_REPAIR

* **Duplicate-edge rejection is correct but not sufficient.** At the R04 trust boundary, `adjustment_set`, `conditioned_on`, and `unmeasured_confounders` must be detached as JSON arrays before use. Scalar strings, `bytes`/`bytearray`, mappings, nested arrays, and non-string members must fail with the existing R04 malformed-input refusal; no `str()` coercion is permissible. Every identifier that contributes to confounder or collider assessment must resolve to a declared graph node—including latent/unmeasured confounder nodes—or fail through the existing unresolved-node path. Otherwise `"ABC"` can become three identifiers, mapping keys can masquerade as members, and arbitrary unknown IDs can alter identification. Schema-permitted duplicates may be normalized only after type and reference validation.

* **The current lag rule is a concrete causal-overclaim path.** A schema-valid value such as `"garbage"` or `"later-ish"` does not establish temporal precedence merely because it is nonblank. R04 must preserve the raw lag but classify the four existing placeholders—and every other string not recognized by higher authority—as **not establishing time order**, thereby lowering the causal ceiling. It must not locally invent ISO-8601, positivity, direction, or unit parsing. Positive interpretation remains `SPEC_GAP: MECHANISM_GRAPH_LAG_SEMANTICS`, owned by the canonical MechanismGraph contract/schema authority; until that contract is frozen, unparsed lag text cannot support `IDENTIFIED`. Missing shared semantics must remain a `SPEC_GAP`, while a conservative refusal or lower ceiling is locally authorized. 

* **Caller-owned aliasing and primitive coercion are material.** Each public graph-builder, gate-builder, and gate-validator boundary must recursively snapshot its complete input exactly once before hashes or semantic reads. The snapshot must reject cycles, byte-like values, non-finite numbers, non-string or duplicate projected keys, and primitive/container hybrids; string and numeric subclasses must retain their underlying primitive values rather than invoke caller overrides. Exact top-level and nested field sets must be checked on the detached snapshot. Graph hash verification, confounder/collider/time-order assessment, mode reduction, identification derivation, and final sealing must then use only that snapshot.

* **Shallow mode and assessment validation is also material.** The validator must revalidate the closed shape and vocabulary of each assessment, require exact binding to the same MechanismGraph identity/hash, require exactly one `INDUCTIVE`, one `DEDUCTIVE`, and one `ABDUCTIVE` verdict, and recompute the permitted causal ceiling and identification status. A rehashed nested verdict must never raise the ceiling or replace a failed assessment. This directly implements R04’s two exit criteria and the constitutional separation of induction, deduction, abduction, and causal identification.  

* **`gate_id` requires no invented rule.** Self-hash verification alone is not semantic authority, but an opaque caller-assigned `gate_id` is not itself a causal overclaim. Re-derive it only if the current R04 builder already defines a deterministic preimage; in that case validator parity and a local `GATE_ID_MISMATCH` refusal are authorized. If no such preimage exists in higher or existing local authority, do not invent one—the mandatory protection is full semantic re-derivation followed by the existing gate-hash check.

All changes remain within `python/epistemic_foundry/reasoning/causal/**`; the existing duplicate-edge guard is retained unchanged.
