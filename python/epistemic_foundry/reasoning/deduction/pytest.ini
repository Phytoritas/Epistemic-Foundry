# R02 reuses the R01 canonical vocabularies from a sibling top-level component
# package, which a relative import cannot cross.  Putting the component root on
# the path makes the absolute import resolve exactly as it does for a consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
