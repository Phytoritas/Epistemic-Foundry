# R01 consumes the O03 EvidencePack contracts from a sibling top-level
# component package, which a relative import cannot cross.  Putting the
# component root on the path makes the absolute import resolve exactly as it
# does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
