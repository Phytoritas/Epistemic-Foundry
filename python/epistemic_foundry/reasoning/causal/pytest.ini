# R04 validates the canonical ArgumentGraph alongside its MechanismGraph, and
# reuses sibling top-level component packages that a relative import cannot
# cross.  Putting the component root on the path makes the absolute import
# resolve exactly as it does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
