# T04 reads the canonical vocabularies from schemas/ at the repository root, so
# the tests resolve that root from the component path rather than an installed
# package.  Putting the component root on the path makes the absolute import
# resolve exactly as it does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
