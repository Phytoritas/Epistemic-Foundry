# P01 reads each role's Evidence ACL from manifests/role_registry.yaml and
# validates briefs against the canonical CouncilBrief schema, so the component
# root goes on the path exactly as it does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
