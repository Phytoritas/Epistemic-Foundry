# P03 reads its vocabularies from the canonical cross-examination and
# minority-report schemas and reuses the P01 blind fixtures, so the component
# root goes on the path exactly as it does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
