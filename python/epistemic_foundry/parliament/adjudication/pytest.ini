# P04 reads its vocabularies from the canonical adjudication and attestation
# schemas and reuses the P02/P03 fixtures, so the component root goes on the
# path exactly as it does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
