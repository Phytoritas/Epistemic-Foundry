# P02 reads the promotion ladder from the canonical passport schema, so the
# component root goes on the path exactly as it does for a component consumer.
[pytest]
testpaths = .
addopts = -q
pythonpath = ../../..
