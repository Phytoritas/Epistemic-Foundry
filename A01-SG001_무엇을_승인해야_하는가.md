# A01-SG001 — 소스 루트 분열과 executor 결합 경계

## 한 줄 요약

독립된 두 가지 결함을 함께 승인해야 합니다.

**첫째, 소스 루트가 둘입니다.** Python 구현이 `python/epistemic_foundry`와
`src/epistemic_foundry` 두 곳에 있는데, wheel에 담기고 기본 `pythonpath`에
오르는 것은 `src` 하나뿐입니다. `python` 쪽 1,196,132바이트는 개발 환경에서
실행되고 일부는 봉인된 증거도 갖고 있지만, 배포 런타임의 import 경로에는
없습니다. 둘을 합칠 규칙이 어떤 계약에도 없습니다.

**둘째, 그리고 더 결정적으로, 결합이 입증된 코드 기반 노드가 하나도
없습니다.** 가장 가까운 것은 K01의 `register_document`로, canonical
`NodeInvocation`을 검증하고 receipt에 결합된 결과를 돌려줍니다. 그러나 이것조차
아래 기준을 충족하지 않습니다. 노드가 `executor_status`를 선언하지 않아 스키마
정의상 "미검증"이고, `expected_output_schema_hash`를 읽지도 비교하지도
않습니다. 나머지는 비즈니스 함수가 있어도 노드에 결합돼 있지 않습니다.

두 번째가 중요한 이유는 **파일을 옮긴다고 결합되지 않기** 때문입니다. 두 축은
서로 독립입니다.

| 소스 상태 | 결합 상태 | 결과 |
|---|---|---|
| 미배포 | 어댑터 있음 | 설치 제품에서 실행 불가 |
| 배포됨 | 원시 비즈니스 함수뿐 | import는 되지만 노드 executor 아님 |
| 미배포 | 원시 비즈니스 함수뿐 | 이관과 결합 둘 다 없음 |
| 배포됨 | 동작 검증된 어댑터 | 실행 후보 |

따라서 1.2MB를 전부 `src`로 옮겨도 실행 가능한 노드 수는 그대로일 수
있습니다. 이관은 배포와 중복 제거를 위해 필요하지만, **워크플로 능력을 여는
주된 열쇠는 아닙니다.**

## 이 문서의 사실 기준

아래 수치는 **현재 working tree**를 기준으로 하며, `HEAD`가 아닙니다. 현재
트리는 이전 세션들의 미커밋 변경을 다수 포함합니다. 같은 규칙을 `HEAD`에
적용하면 1,074,944바이트로 나오고, `python` 프로덕션 파일 53개 중 29개가
`HEAD`와 다릅니다. 예를 들어 현재 K03 `emitter.py`는 30,303바이트인데 봉인된
K03 시도의 기록은 27,921바이트입니다.

따라서 "1.2MB 전체가 통과·봉인됐다"는 뜻이 아닙니다. 각 패키지의 봉인 증거는
**기록 시점의 바이트**를 증명하며, 현재 바이트를 증명하지 않습니다. 승인 시
기준 커밋 또는 스냅샷 해시를 함께 고정해야 합니다.

바이트 집계 규칙: `python/epistemic_foundry` 아래 `.py` 파일 중
`__pycache__`, `test_*.py`, `conftest.py`, `fixtures.py`를 제외한 것.

## 왜 이제야 문제가 되는가

지금까지는 각 work package가 자기 트리 안에서 완결됐기 때문에 드러나지
않았습니다. 워크플로가 실제로 실행 가능한지 묻기 시작하면서 드러났습니다.

구체적 계기는 O02였습니다. O02의 write scope는 원래
`python/epistemic_foundry/retrieval/lanes/**`뿐이었는데, 실제로 동작하는 lane
구현은 `src/epistemic_foundry/retrieval/lanes.py`에 있었습니다. 이 한 건은
현재 매니페스트에서 두 `src` 경로를 추가해 교정했지만, 같은 상태의 패키지가
24개 더 있습니다.

## 확인된 사실

### 실행되는 것은 `src` 하나뿐입니다

`pyproject.toml`의 관련 발췌입니다. 연속된 인용이 아니며 순서를 재배열했습니다.

```toml
[tool.setuptools.package-dir]
"" = "src"
[tool.setuptools.packages.find]
where = ["src"]
[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.epistemic-foundry.workspace]
python_runtime_root = "src/epistemic_foundry"
python_component_root = "python/epistemic_foundry"
component_source_imports = "forbidden"
```

`packages/boundary-policy.json`도 같은 값을 선언하고
`duplicateImplementationPolicy: "forbidden"`을 추가합니다.

정확히 말하면 `python` 트리는 **현재 wheel과 기본 import 경로에 포함되지
않습니다**. 전면 금지는 아닙니다. 패키지별 pytest 설정은 컴포넌트 루트를
명시적으로 `pythonpath`에 올리고, C02 검증기는 `python/.../contracts/models.py`를
직접 로드합니다. `component_source_imports = "forbidden"`이 실제로 막는 것은
`sys.path` 변경과 파일시스템 경유 소스 import 우회입니다.

따라서 `PYTHONPATH=python`으로 동작한다는 사실은 개발 환경 실행을 증명하지만,
제품이 그 코드를 구현했다는 증거는 아닙니다.

### 도달 불가능한 코드의 규모

| 항목 | 값 |
|---|---:|
| `python/epistemic_foundry` 프로덕션 바이트 | 1,196,132 |
| 프로덕션 파일 | 53 |
| 최상위 서브패키지 | 11 |

서브패키지: contracts, ingest, intake, ontology, parliament, reasoning,
reassessment, retrieval, storage, tools, validation.

### 워크플로 executor 도달성

23개 워크플로의 350개 노드 중 265개가 Python `module:symbol` executor를
지정합니다. 여기에는 두 가지 별개의 질문이 있고, 답이 다릅니다.

**모듈이 존재하는가** (경로만 확인):

| 모듈이 있는 곳 | 개수 |
|---|---:|
| `src`만 | 24 |
| 양쪽 | 12 |
| `python`만 | 5 |
| 어느 쪽도 아님 | 224 |

**심볼까지 존재하는가** (AST로 top-level def/class/assign/import 확인):

| 심볼이 있는 곳 | 개수 |
|---|---:|
| `src`만 | 24 |
| 양쪽 | 1 |
| `python`만 | 4 |
| 어느 쪽도 아님 | 236 |

두 표의 차이가 중요합니다. `양쪽 12`는 모듈 이름이 양쪽에 있다는 뜻일 뿐이고,
실제로 양쪽에 심볼이 있는 것은 `ingest.registry:register_document` 하나입니다.
나머지 11개는 `retrieval.lanes`의 lane 이름들인데, `python` 패키지는 그 심볼을
하나도 export하지 않고 `src`도 `lexical`, `citation`, `entity_variable` 세 개만
갖고 있습니다.

`python`에만 있고 심볼도 실재하는 4개:

- `epistemic_foundry.ingest.spans:emit` (`corpus_ingest.workflow.yaml:309`)
- `epistemic_foundry.retrieval.planning:compile_query_plan` (`evidence_retrieval.workflow.yaml:123`)
- `epistemic_foundry.retrieval.planning:seal_search_lane_receipt` (`:691`)
- `epistemic_foundry.retrieval.planning:reconcile_search_run` (`:907`)

**다만 이 네 개도 옮긴다고 워크플로가 실행되지는 않습니다.** 노드는
`NodeInvocation`을 받아 `ResultEnvelope`를 반환한다고 선언하는데, 실제 함수
시그니처는 다릅니다. `emit`은 `(SourceSnapshot, Sequence[SpanCandidate]) ->
tuple[SourceSpan, ...]`이고, `compile_query_plan`은 QueryPlan proposal을 받아
`SealedArtifact`를 반환합니다. 워크플로 컴파일러도 참조 형식만 검사하고
import·callable·시그니처를 확인하지 않습니다. 따라서 이들은 "python에만 있는
executor"가 아니라 "python에만 있는 모듈 참조"이며, 각각 별도의 executor 계약
결합 판정이 필요합니다.

236개가 어느 쪽도 아니라는 사실이 더 중요합니다. 워크플로 계층의 대부분은 아직
명세일 뿐입니다. **트리 통합은 그 236개를 줄이지 않습니다.**

### 결합 상태 조사

해석되는 29개 callable이 선언된 노드 계약과 맞는지 대조했습니다. 모든 노드는
`input_schema_ref: node-invocation.schema.json`과 각자의 `output_schema_ref`를
선언하는데, 실제 시그니처는 전부 다릅니다.

```text
gate_g08_adaptive_statistics
  선언: node-invocation -> gate-decision
  실제: (_payload: Mapping[str, Any]) -> dict[str, Any]

ingest.spans:emit
  선언: node-invocation -> result-envelope
  실제: (snapshot: SourceSnapshot, candidates: Sequence[SpanCandidate])
        -> tuple[SourceSpan, ...]
```

다만 시그니처 대조는 진단일 뿐 판정 기준이 아닙니다. 정적 시그니처는 노드 ID
결합, 입력 해시 무결성, 인가, 선언 스키마 출력, 멱등성, 효과 조정, 재생 시
재호출 없음을 증명하지 못합니다. 반대로 팩토리나 주입된 서비스는 단순 애노테이션
없이도 계약을 만족할 수 있습니다.

따라서 정직한 행위 기준 집계는 다음입니다.

| 상태 | 개수 |
|---|---:|
| 모듈 없음 | 224 |
| 모듈은 있으나 심볼 없음 | 12 |
| callable은 있으나 결합 미입증 | 29 |
| 행위로 결합이 입증됨 | 0 |
| **합계** | **265** |

이 집계는 Python `module:symbol` 참조 265개만 다룹니다. 나머지 85개 노드
(prompt 75, subworkflow 9, tool 1)는 파일은 존재하지만 결합 행위를 조사하지
않았습니다. 따라서 "350개 중 0개 결합"이라고 말할 근거는 아직 없고, 정확히는
"코드 기반 265개 중 0개 결합"입니다.

가장 가까운 것은 K01의 `register_document`입니다
(`src/epistemic_foundry/ingest/registry/service.py`). 이것은 invocation을
canonical 스키마로 검증하고, `node_id`를 확인하고, `input_hash`를 봉인된 요청
해시에 결합하고, 주입된 port로 입력 아티팩트를 해석하고, receipt에 결합된
결과를 돌려줍니다.

**그러나 K01도 `BOUND`가 아닙니다.** 아래 결정 L의 기준으로 판정하면:

- 노드가 `executor_status`를 선언하지 않습니다
  (`corpus_ingest.workflow.yaml`의 `register_document` 노드). 스키마 정의상
  부재는 "미검증"이며 게이트는 통과가 아니라 인구조사 수치로 보고합니다.
- `expected_output_schema_hash`를 읽지도 실제 스키마 해시와 비교하지도
  않습니다. 테스트가 임의의 `sha256:111…`을 넣어도 통과합니다.
- 봉인된 K01 증거의 `service.py` 해시가 현재 파일과 다릅니다.

따라서 K01은 **참조 오라클(reference oracle)**이지 결합 사례가 아닙니다.
무엇이 노드 결합인지 보여주는 가장 완전한 예시로서 가치가 있지만, 그 자체가
결합됐다고 세면 안 됩니다.

워크플로 컴파일러는 `executor_ref` 문자열 형식만 검사하고 import나 시그니처를
확인하지 않습니다.

`register_document`의 반환 모양을 모든 노드에 일반화하면 안 됩니다.
`compile_query_plan` 노드는 `query-plan.schema.json`을 출력으로 선언하고,
retrieval lane은 `result-envelope.schema.json`을 선언합니다. 선언된
`output_schema_ref`가 권위이며, `ResultEnvelope`는 그것이 선언된 출력이거나
워크플로가 별도 실행 사이드카로 규정한 경우에만 만들어집니다.

### 결합 상태 분류

```text
ingest.registry:register_document       → REFERENCE_ORACLE + BINDING_UNVERIFIED
ingest.spans:emit                       → BUSINESS_CALLABLE_ONLY + TRANSITION_ONLY_SOURCE
retrieval.planning:compile_query_plan   → BUSINESS_CALLABLE_ONLY + TRANSITION_ONLY_SOURCE
retrieval.lanes:lexical                 → BUSINESS_CALLABLE_ONLY (src에서 import돼도)
validation.reconcile:evidence           → MISSING_SYMBOL
```

### 이미 있는데 아무도 쓰지 않는 필드

조사 중 확인한 사실입니다. `schemas/node-contract.schema.json`에는 이미
`executor_status` 필드가 있고 값은 `executor_bound` / `executor_unbound`입니다.
스키마의 설명문이 정확히 이 문제를 겨냥합니다.

> `executor_ref`가 이 릴리스에서 실제 callable로 해석되는지 여부.
> `executor_bound`는 참조가 살아 있다고 주장하며 executor-resolution 게이트가
> 검사한다. `executor_unbound`는 명세됐지만 구현되지 않은 노드를 선언한다.
> 부재는 미검증이며, 게이트는 이를 통과가 아니라 인구조사 수치로 보고한다.

워크플로 컴파일러도 이 필드를 검증하고 인구조사를 산출합니다
(`workflow-compiler.mjs:120,158-161`).

**그런데 350개 노드 중 이 필드를 선언한 것이 하나도 없습니다.** 전부 "미검증"
상태이고, 따라서 게이트는 아무것도 실패시키지 않습니다. 결합 여부를 선언 가능하게
만드는 장치가 이미 있는데 사용되지 않고 있습니다.

이는 결합 경계 결정이 완전히 새로운 계약을 발명하는 것이 아님을 뜻합니다.
어휘와 검증기는 이미 존재하고, 없는 것은 각 노드가 자기 상태를 선언하는 것과
무엇이 `executor_bound`를 정당화하는지에 대한 합의입니다.

### 중복 모듈 정체성 4건

`duplicateImplementationPolicy: forbidden`을 이미 위반하고 있습니다.

- `epistemic_foundry.contracts`
- `epistemic_foundry.ingest.registry` — 양쪽 모두 `register_document`를 export하며 **구현이 다릅니다**
- `epistemic_foundry.retrieval`
- `epistemic_foundry.retrieval.lanes` — 한쪽은 패키지, 한쪽은 모듈 파일

### 매니페스트 분포

- `python/epistemic_foundry/**`만 선언: 24개 패키지
- `src/epistemic_foundry/**`만 선언: 43개 패키지
- 양쪽 선언: O02 1개 (이번에 손으로 교정)

### 승격 경로가 없습니다

- `python/README.md`는 "staged monorepo transition"이라고만 하고 담당 패키지,
  명령, 소유자를 정하지 않습니다.
- B01 보고서는 "later package가 고유 소유 모듈을 추가할 때까지"라고만 적었고,
  승격을 수행할 패키지를 특정하지 않았습니다. 이후 여러 패키지가 실제로 모듈을
  추가했지만 승격 담당은 여전히 없습니다.
- B05/B06 scope는 `build/v4_b05|b06/**`와 각자의 `artifacts/work_packages/**`
  뿐입니다.
- 실제 빌드 materializer는 `schemas/`와 `openapi/`만 `src/.../_canonical`로
  투영합니다. 컴포넌트 소스는 옮기지 않습니다.
- 승격을 수행하거나 검증하는 워크플로 노드와 repo-check가 없습니다.

## 왜 코드를 그냥 옮기면 안 되는가

세 가지입니다.

**첫째, 권한이 없습니다.** 24개 패키지의 소유 경로를 바꾸려면 매니페스트를
고쳐야 하는데, `manifests/development_manifest.yaml`을 write scope에 선언한
패키지가 하나도 없습니다. 매니페스트를 누가 소유할지를 그 매니페스트 안에서
정하는 건 순환이므로, 이 결정은 상위 권위에서 내려와야 합니다.

**둘째, 중복 4건은 의미 병합입니다.** `ingest.registry`의 두
`register_document`는 이름만 같고 계약이 다릅니다. `src` 쪽은 `NodeInvocation`과
주입된 durable port를 받아 receipt에 결합된 `ResultEnvelope`를 돌려주고,
`python` 쪽은 in-memory registry에 소스 바이트와 manifest를 넣는 API입니다.
"더 새것", "더 큰 것", "이미 패키징된 것" 어느 것도 자동으로 이기지 않습니다.
어느 쪽이 맞는지는 해당 도메인 패키지가 판단해야 합니다.

**셋째, 봉인된 증거가 어떻게 되는지 정해져 있지 않습니다.** 예를 들어 K03은
`python/.../ingest/spans/emitter.py`에 대해 36개 테스트 통과를 기록한
`report.json`, `commands.jsonl`, `review.md`를 갖고 있습니다. 파일이 옮겨지면
그 기록의 경로가 틀려집니다. 기존 증거를 덮어쓸지, 후속 시도를 만들지 규칙이
필요합니다.

## 권장 결정

ChatGPT Pro의 답변(`docs/research/pro-answer-python-src-split.md`)과 이후
독립 검토를 반영한 안입니다.

### 결정 A — `src`가 정식 루트로 살아남습니다

`src/epistemic_foundry`가 유일한 설치·테스트·워크플로 해석·배포 대상
구현 루트입니다. `python/epistemic_foundry`는 이관 전용 소스 재고이며, 배출 후
폐기합니다.

반대 방향(=`python`을 패키징 루트로)은 결함을 뒤집을 뿐입니다. 현재 `src`
구현이 도달 불가능해지고, 중복 4건은 그대로 남으며, 런타임·CLI·테스트 가정이
전부 뒤집힙니다.

### 결정 B — 두 루트를 다리로 잇지 않습니다

다음은 모두 금지합니다. 결함을 감출 뿐 해결하지 않습니다.

- 두 루트를 함께 패키징
- `sys.path`에 `python/` 추가
- namespace package 병합
- symlink 또는 `.pth`
- 런타임에 컴포넌트 소스 복사

### 결정 C — 이관 단위는 원래 work package입니다

최상위 서브패키지 단위가 아닙니다. `ingest`나 `retrieval` 하나에 여러 패키지의
작업이 섞여 있어서, 통째로 옮기면 의미 권한이 조용히 이전됩니다. 반대로 1.2MB를
한 번에 옮기는 거대 패키지는 K03, O02, V04 등의 인수 계약을 만족하지 않고
승자를 정하게 됩니다.

한 dotted module 정체성은 원자적으로 옮깁니다.

```text
승인된 src 후상태
+ 대응하는 python 구현 폐기
+ 패키지 테스트
+ 설치 런타임 import 테스트
+ 해당되면 워크플로 해석 테스트
+ 중복 정체성 전수 조사
+ 후속 증거
```

이관 도중 두 루트에 동시에 남겨두지 않습니다.

### 결정 D — 중복은 명시적 의미 병합입니다

경로, 시각, 패키징 여부, 테스트 수가 승자를 정하지 않습니다. 소유 패키지가
두 구현의 의미를 비교해 하나의 `src` 후상태를 만듭니다. 두 패키지가 각각
한쪽을 소유한다고 볼 수 있으면 의미 저작권과 병합 책임이 배정될 때까지
`SPEC_GAP`으로 멈춥니다.

### 결정 E — 기존 증거는 불변입니다

K03 증거는 소급해서 거짓이 되지 않습니다. 그것은 "기록된 시점에 그 경로에
구현이 있었고 기록된 테스트가 통과했다"를 증명합니다. 증명하지 않는 것은
"현재 설치 런타임에서 도달 가능하다"입니다.

따라서 이동은 후속 시도(successor attempt)를 만들고, 기존 시도를 다시 쓰지
않습니다. 후속 증거에는 최소한 이전 시도 식별자, 옛 경로와 해시, 새 경로와
해시, 이관 분류(`BYTE_PRESERVING_MOVE` 또는 `SEMANTIC_MERGE`), 선택된 의미
후상태와 근거, 폐기된 경로, 설치 wheel의 모듈 출처, 재실행한 테스트 결과,
워크플로 해석 결과, 중복 전수 조사, 독립 검토를 기록합니다.

### 결정 F — 소유권 분리

| 책임 | 소유자 | 경계 |
|---|---|---|
| 살아남을 루트 선택과 이관 의미 동결 | A01 | `MASTER_SPEC.md` |
| 개발 매니페스트 투영 유지 | A04 | `manifests/development_manifest.yaml` |
| Python 빌드와 루트 경계 기계 | B01 | `pyproject.toml`, 루트/경계 기반 |
| 이관된 코드의 의미 선택 | 원래 소유 패키지 | 자기 모듈과 테스트 |
| 구현이 다른 중복의 조정 | 의미 소유자 또는 관련 소유자 전원 | 명시적 병합 결정 |

A04는 매니페스트 투영의 관리인이지, 그 매니페스트가 투영하는 모든 패키지의
의미 소유자가 되는 것이 아닙니다. B01도 `python/**`를 스캐폴드 수준에서
소유한다는 이유로 두 `register_document` 중 하나를 고를 수 없습니다.

### 결정 G — 이관 순서, 롤백, 승인 형식

이관은 임의 순서로 할 수 없습니다. 컴포넌트끼리 서로 import합니다. 예를 들어
`validation/reconcile`은 `validation.execution`, `validation.planning`,
`validation.targets`를 직접 import하므로 V04를 먼저 옮기면 깨집니다. 또한
프로덕션 `.py` 16개가 저장소 루트의 `schemas/` 경로를 참조하는데, 설치된
패키지에서는 그 경로가 다릅니다. 단순 이동이 아니라 리소스 해석 방식 전환이
필요할 수 있습니다. (일부는 생성된 모델의 스키마 ID 문자열이므로, 실제 런타임
파일시스템 읽기만 별도로 집계해야 합니다.)

따라서 이관은 dependency-ready 순서로 직렬 실행하고, 각 단계는 실패 시 원래
루트를 복원하는 전체 롤백을 갖습니다. 부분 이동 상태로 멈추지 않습니다.

승인 자체도 Foundry 계약을 따릅니다. 인간 승인은 열람한 리비전, 근거, 범위,
무효화되는 하위 항목을 담은 불변 기록이어야 합니다. 이 문서를 승인할 때
기준 커밋 또는 스냅샷 해시를 함께 고정해야, 나중에 "무엇을 승인했는가"가
확정됩니다.

## 두 번째 결정 — executor 결합 경계

소스 루트 결정과 독립적으로, 같은 A01 개정에서 다음도 동결해야 합니다.

### 결정 H — 소스 도달성과 노드 결합은 별개 축입니다

모듈이나 callable이 해석된다는 사실이 워크플로 노드 결합을 증명하지 않습니다.

### 결정 I — `executor_ref`의 의미를 고정합니다

코드 기반 노드에서 `executor_ref`는 **스케줄러가 부르는 노드 어댑터
진입점**을 가리킵니다. 원시 과학·도메인 함수는 비즈니스 구현이며, 별도로 결합된
구현 정체성으로 보존됩니다.

`emit(snapshot, candidates) -> tuple[SourceSpan, ...]`은 훌륭하고 테스트를
통과하더라도, 어댑터가 없는 한 그 노드가 부르는 callable이 아닙니다.

이는 현재 `executor_ref` 필드에 적힌 참조 상당수가 실제로는 **아직 결합되지
않은 비즈니스 참조**라는 뜻입니다. 이 의미 정정은 나중에 노드별 워크플로 또는
결합 기록 변경을 요구합니다. 기존 원시 심볼을 실행 가능하다고 선언해서 감추면
안 됩니다.

### 결정 J — 공유 셸과 노드별 어댑터로 나눕니다

한쪽 극단은 모두 틀립니다. K01 전체 수명주기를 패키지마다 다시 구현하면 보안,
재생, 효과 의미가 중복되고 반드시 어긋납니다. 반대로 임의 함수 호출 방법을
추측하는 범용 리플렉션 래퍼는 도메인 의미를 조용히 결정하고, 결합되지 않은
callable을 결합된 것처럼 보이게 만듭니다.

| 책임 | 소유자 |
|---|---|
| 규범 정의와 소유권 배정 | A01 / 제품 소유자 |
| 공유 워크플로 런타임 셸 | W02 (A01의 명시적 위임 후) |
| 컴파일 시점 결합 검증과 fail-closed 거부 | W01 |
| 노드별 입력 매핑, 비즈니스 호출, 출력 매핑, 도메인 실패 | 원래 도메인 패키지 |
| ActionIntent, Attempt, 멱등성, 효과 조정 | E02 (기존 권위) |
| capability, lease, fencing, 승인, 정책 | E03 (기존 권위) |

이 배정 중 **W02만 실제 확장입니다.** W02는 이미
`packages/foundry-kernel/src/workflows/runtime/**`를 소유하므로 경로는 맞지만,
선언된 제목과 인수 기준은 "체크포인트, 일시정지, 재개, 취소"뿐입니다. 공유 실행
셸을 맡기려면 W02의 제목과 인수 기준을 함께 넓혀야 합니다. 이는 A01 결정의
일부이며, 현재 매니페스트만으로는 도출할 수 없습니다.

나머지는 기존 소유와 일치합니다. W01은 `workflows/compiler/**`, E02는
`effects/**`, E03은 `capabilities/**`를 이미 소유합니다.

공유 셸이 하는 일: invocation 검증, 정확한 NodeContract·결합 해석, 불변 입력과
해시 검증, 범위·취소·마감 강제, 멱등성/재생 조정, 출력 스키마 검증, 선언된 효과
조정. 하지 않는 일: 도메인 인자를 무엇으로 채울지 결정, 효과나 lease 로직 재구현.

노드별 어댑터가 하는 일: 아티팩트 역할을 도메인 인자로 매핑, canonical 데이터를
도메인 객체로 디코딩, 정확한 비즈니스 함수와 고정 옵션 선택, JSON Schema보다
강한 도메인 불변식 강제, 도메인 결과를 선언된 출력으로 변환, 도메인 오류를
기존 실패 어휘로 매핑. 하지 않는 일: 자기 인가, capability/lease 발급, 다른
NodeContract 선택, 멱등성 우회, 검증 없는 반환값을 성공 봉투로 전환.

### 결정 K — 선언 출력과 비즈니스 출력을 구분합니다

`ResultEnvelope`는 그것이 선언된 출력이거나 워크플로가 별도 실행 사이드카로
명시한 경우에만 만들어집니다. 봉투가 비즈니스 아티팩트를 대체하지 않습니다.
따라서 "`NodeInvocation`을 받고 `ResultEnvelope`를 반환한다"는 보편적 정적
시험이 될 수 없습니다.

실제 분포가 이를 뒷받침합니다. 350개 노드 전부가 `output_schema_ref`를
선언하는데, 206개가 `result-envelope`이고 나머지 144개(41.1%)는 64종의 다른
스키마를 선언합니다. 예를 들어 `compile_query_plan`은 `query-plan`을,
`issue_search_completeness_certificate`는 `search-completeness-certificate`를
선언합니다.

**다만 `output_schema_ref`가 항상 비즈니스 출력인 것은 아닙니다.** 워크플로가
봉투를 사이드카로 규정한 경우가 있습니다. `evidence_retrieval.workflow.yaml`의
`retrieval_candidate_contract`는 다음을 별도로 선언합니다.

```yaml
business_output_schema_ref: schemas/retrieval-candidate.schema.json
execution_envelope_schema_ref: schemas/result-envelope.schema.json
result_envelope_role: telemetry_sidecar_only
result_envelope_output_binding: output_artifact_ids
```

이 경우 lane 노드의 `output_schema_ref`는 `result-envelope`이지만 실제 비즈니스
산출물은 `RetrievalCandidate`입니다. 따라서 정확한 규칙은 다음입니다.

- `output_schema_ref`는 **노드가 직접 반환하는 선언 출력**의 권위입니다.
- 워크플로가 봉투를 사이드카로 규정한 경우, `business_output_schema_ref`도
  정확한 해시로 결합·검증해야 합니다.
- 봉투만 검증하고 비즈니스 아티팩트를 검증하지 않는 셸은 틀립니다.

### 결정 L — `BOUND`의 기준

다음으로는 `BOUND`를 확립할 수 없습니다: 정적 시그니처 호환, import 성공, 심볼
별칭, node-ID 디스패치 테이블, 와일드카드 래퍼, `sys.path` 변경, 그럴듯한 딕셔너리.

`BOUND`는 다음을 모두 요구합니다: 깨끗한 설치 런타임에서의 해석, 해시로 봉인된
정확한 노드 결합, canonical invocation 성공, 정확한 출력 스키마 검증, fail-closed
음성 사례, 선언된 효과 조정, 해당되는 경우 재생 시 비즈니스 재호출 없음.

### 결정 M — 결합 기록의 최소 정체성

`BOUND`가 "해시로 봉인된 정확한 결합"을 요구하려면 무엇을 해시하는지 정해야
합니다. 개념적 최소 정체성은 다음입니다.

```text
workflow_id, workflow_version
node_id, node_contract_hash
adapter_ref, adapter_revision_or_digest
business_refs[], business_revision_or_digests[]
input_schema_ref, input_schema_hash
output_schema_ref, output_schema_hash
binding_hash
```

**여기에 capability, read/write scope, expected_effects, required_policy_checks를
중복 기재하지 않습니다.** `schemas/node-contract.schema.json`이 이미 이 필드들을
required로 소유합니다. 두 곳에 두면 정책 출처가 둘이 되어 어긋날 수 있습니다.

기계 판독 가능한 표현(스키마 파일)은 나중에 위임합니다. 지금 만들면 A01의 write
scope를 넘습니다.

### 결정 N — 기존 N02 실행 코드의 처분

현재 워킹 트리에는 미추적 상태의 66KB 실행 경계가 있습니다
(`packages/role-router/src/adapters/execute-bounded-adapter-invocation.mjs`).
이 코드는 invocation 검증, 출력 스키마 해시 결합, 재생 조기 반환, 어댑터 호출,
영수증과 `ResultEnvelope` 검증을 이미 구현합니다.

다만 canonical 워크플로 `executor_ref`에 결합돼 있지 않고, package export도
비어 있으며, 활성 테스트/증거가 없습니다. 따라서 결합 사례로 세지 않습니다.

그러나 W02가 이를 구성해 쓸지, 이관할지, 폐기할지 정하지 않으면 실행 셸이 둘이
됩니다. A01 결정에 W02와 N02의 경계와 이 코드의 처분을 포함해야 합니다.

### 결정 O — 위임 전까지는 `SPEC_GAP`

공유 셸 소유자와 결합 표현이 위임되기 전까지, 구현은 로컬 어댑터 관행을
발명하는 대신 `SPEC_GAP`을 반환합니다.

## 승인 후 첫 단계 — 코드는 옮기지 않습니다

첫 단계는 K03 이관이 아니라 **A01이 위 규칙을 `MASTER_SPEC.md`에 동결하는
후속 시도**입니다.

- 소유자: A01
- 규범 파일: `MASTER_SPEC.md`
- 증거: A01 후속 시도
- 옮기는 코드: 없음
- 바꾸는 워크플로: 없음
- 실행하는 이관: 없음

지금 K03을 옮기면 다음을 전부 권한 없이 암묵 결정하게 됩니다. `src`가 최종
루트라는 것, 봉인된 증거가 경로 이동에서 어떻게 살아남는지, 옮겨진 패키지를
다시 여는지 이름만 바꾸는지, 누가 매니페스트를 고칠 수 있는지, 중복을 복사할지
교체할지 병합할지, 이관 후 `python/`이 여전히 허용되는지.

## 승인이 필요한 최소 결정

1. `src/epistemic_foundry`가 유일한 정식 Python 구현 루트입니다.
2. `python/epistemic_foundry`는 이관 전용이며 배출 후 폐기합니다.
3. 두 루트를 잇는 런타임·빌드 다리를 금지합니다.
4. 이관 단위는 원래 work package이고, dotted module 정체성은 원자적입니다.
5. 구현이 다른 중복은 명시적 의미 병합이며 경로가 승자를 정하지 않습니다.
6. 기존 봉인 증거는 불변이고, 이동은 후속 시도를 만듭니다.
7. A04가 개발 매니페스트 관리인, B01이 빌드 루트 기계 소유자입니다.
8. 이관 완료 전까지 `python`에만 있는 모듈 참조는 배포 런타임에서 미결합이며,
   중복 정체성은 정책 위반으로 남습니다.
9. 첫 단계는 A01의 `MASTER_SPEC.md` 규칙 동결이고 코드는 옮기지 않습니다.
10. 이관은 dependency-ready 순서로 직렬 실행하고 실패 시 전체 롤백합니다.
11. 이 승인에 기준 커밋 또는 스냅샷 해시를 함께 고정합니다.
12. 소스 도달성과 노드 결합은 독립된 성숙도 축입니다.
13. `executor_ref`는 노드 어댑터 진입점을 가리키며, 원시 비즈니스 함수는 별도
    결합 정체성으로 보존됩니다.
14. 공유 런타임 셸은 W02가, 컴파일 시점 결합 검증은 W01이, 노드별 어댑터는
    원래 도메인 패키지가, 효과는 E02가, capability는 E03이 소유합니다.
15. `output_schema_ref`는 노드가 직접 반환하는 선언 출력의 권위이며,
    워크플로가 봉투를 사이드카로 규정한 경우 `business_output_schema_ref`도
    해시로 결합·검증합니다. `ResultEnvelope`는 보편 반환형이 아닙니다.
16. `BOUND`는 설치 런타임 해석, 해시 봉인 결합, invocation 성공, 출력 스키마
    검증, fail-closed 음성 사례, 효과 조정, 재생 시 재호출 없음을 모두
    요구합니다.
17. 결합 기록의 최소 정체성을 지금 동결하되, 기계 판독 표현(스키마)은
    나중에 위임합니다.
18. W02의 제목과 인수 기준을 공유 실행 셸까지 넓힙니다.
19. 현재 미추적 상태인 N02 bounded executor 코드의 처분과 W02와의 경계를
    정합니다.
20. 결합 표현과 공유 셸 소유자가 위임되기 전까지 구현은 `SPEC_GAP`입니다.

## 승인 후 검증 방법

A01 단계가 실제로 됐다는 증거:

- 시도 시작 시점 트리와 비교한 scoped diff에 `MASTER_SPEC.md`와 A01 후속
  증거만 있음. 현재 트리에는 이전 세션들의 미커밋 변경이 다수 있어 저장소
  전체 `git diff`로는 확인할 수 없습니다.
- 사양 번들 검증기 통과
- 독립 검토자가 `pyproject.toml`을 보지 않고 `MASTER_SPEC.md`만으로 정식 루트,
  컴포넌트 루트 처분, 이관 단위, 원자 단위, 중복 해결 방식, 기존 증거 처분,
  매니페스트 관리인, 빌드 루트 소유자, 모호할 때의 처리를 도출할 수 있음
- 독립 검토자가 K01 구현 세부를 보지 않고 다음을 도출할 수 있음:
  공유 셸 소유자 = W02, 컴파일 시점 검증자 = W01, 노드별 어댑터 소유자 =
  원래 도메인 패키지, 효과 권위 = E02, capability 권위 = E03,
  `callable 존재 ≠ BOUND`, `output_schema_ref`가 출력 권위,
  보편 `ResultEnvelope` = 거짓, 모호한 결합 = `SPEC_GAP`
- 검토자가 K01, K03, O01, `validation.reconcile:evidence`를 위 분류대로 판정함
- 깨끗한 wheel에 여전히 `src` 유래 패키지만 들어 있음
- executor 조사 수치가 그대로임: 모듈 기준 24/12/5/224, 심볼 기준
  24/1/4/236. 규칙 동결 단계가 도달성을
  개선하면 안 됩니다.
- 행위 기준 집계도 그대로임: 224 모듈 없음, 12 심볼 없음, 29 결합 미입증,
  0 결합 입증. 계약 동결 단계에서 수치 개선은 기대하지 않습니다.
- 350개 노드 중 `executor_status`를 선언한 것이 여전히 0개. 계약 동결은
  선언을 만들지 않습니다.
- 미결합 노드는 계속 fail closed
- 중복 4건이 해결된 것이 아니라 미해결 이관 부채로 계속 보임

겉으로만 된 것으로 봐야 하는 신호:

- `PYTHONPATH=python`을 줘야 K03이 import됨
- setuptools가 두 루트를 패키징함
- symlink, import hook, `.pth`, namespace package, `sys.path` 변경으로 다리를 놓음
- `duplicateImplementationPolicy`를 약화하거나 예외 목록을 만듦
- `python/` 구현이 살아 있는 채로 `src`에 복사만 함
- 봉인된 K03/A01 보고서를 제자리에서 수정함
- A01 위임 없이 A04 매니페스트 행만 바뀜
- 규칙 동결 단계에서 executor 해석 수치가 개선됨
- 경로만 바뀐 패키지를 "이관 완료"라고 부르면서 설치 런타임 출처와 워크플로
  호출을 시험하지 않음
- 해석되는 callable 수가 늘어남
- 컴파일러가 mapping이나 `*args`/`**kwargs`를 받는다는 이유로 callable을 수용함
- `resolve_node_executor(node_id)`가 원시 비즈니스 함수로 직접 디스패치함
- 범용 래퍼 하나가 여러 노드에 그럴듯한 봉투를 만들어냄
- 모든 비즈니스 결과를 `ResultEnvelope`로 강제함
- 집계를 초록으로 만들려고 `evidence`를 `reconcile_evidence`에 별칭함
- 이 단계에서 스키마, 워크플로, 매니페스트, 런타임 구현이 하나라도 바뀜

## 함께 보고하는 별건

`validation_execution.workflow.yaml:429`의
`epistemic_foundry.validation.reconcile:evidence`는 어느 트리에도 없는 심볼을
가리킵니다. 공개 API는 `reconcile_evidence`입니다.

다만 값싸게 고칠 수 없습니다. 두 가지 가능성이 있습니다.

1. 워크플로 참조가 틀렸고 `reconcile_evidence`가 맞다 — `reconcile_evidence`가
   별도로 노드 어댑터 계약을 만족한다고 입증됐을 때만 성립합니다.
2. `evidence`가 원래 얇은 executor adapter로 의도됐다 — invocation을 검증하고
   입력을 로드하고 `reconcile_evidence`를 호출하고 출력을 저장하고 envelope을
   구성하는 층입니다.

`evidence = reconcile_evidence` 한 줄로 심볼을 만들면 심볼 해석은 개선되지만
실행 가능성은 그대로입니다. 입력, 출력, 저장, 영수증 의미가 틀린 callable이
됩니다.

게다가 V04는 `python/epistemic_foundry/validation/reconcile/**`만 소유하고,
W01은 워크플로 컴파일러를 소유할 뿐 `validation_execution.workflow.yaml`의 의미
내용을 소유하지 않습니다. 그 워크플로의 의미 소유자가 배정될 때까지
`SPEC_GAP`입니다.

같은 조사에서 "모듈은 있으나 심볼이 어느 트리에도 없는" 참조가 위 건을 포함해
12건 확인됐습니다.

- `claim_extraction.workflow.yaml:383` — `claim_forge.evidence:build`
- `evidence_retrieval.workflow.yaml:215,356,404,452,500,548,596,643` —
  `retrieval.lanes`의 `semantic`, `mechanism`, `counterevidence`, `null`,
  `boundary`, `method`, `temporal`, `external_novelty`
- `evidence_update_reassessment.workflow.yaml:72` — `updates.impact:build`
- `validation_execution.workflow.yaml:191` — `governance.approvals:resolve_validation`
- `validation_execution.workflow.yaml:429` — `validation.reconcile:evidence`

이 12건은 트리 분열과는 별개의 결함군입니다. 이 승인 요청에 포함하지 않습니다.

## 이 문서의 지위

**이 파일이 존재한다는 사실만으로 결정이 승인되지 않습니다.** 제품 소유자가
명시적으로 승인한 뒤에야 A01 후속 시도를 시작할 수 있습니다. 승인 전까지 현재
상태가 그대로 유지됩니다.

```text
A01-SG001: 미해결
python/ 트리: 배포 런타임에서 도달 불가
중복 모듈 정체성 4건: 정책 위반 상태로 존속
행위로 결합이 입증된 노드: 0개
참조 오라클: 1개 (K01 register_document, 결합 미검증)
결합 미입증 callable: 29개
심볼 부재: 12건
모듈 부재: 224건
completion_ready: false
```
