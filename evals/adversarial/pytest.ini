# Q04's adversarial harness, dataset and results artifact live together under
# evals/adversarial, which is outside the repository test path, so the
# component root goes on the path and the suite is targeted only.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
