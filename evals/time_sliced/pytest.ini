# Q04's time-sliced harness, dataset and results artifact live together under
# evals/time_sliced, which is outside the repository test path, so the
# component root goes on the path and the suite is targeted only.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
