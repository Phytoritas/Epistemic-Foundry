# Y04's scale-qualification harness, synthetic corpus and results artifacts live
# together under evals/scale, which is outside the repository test path, so the
# component root goes on the path and the suite is targeted only.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
