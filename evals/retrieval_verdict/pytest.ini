# Q03's evaluator and corpus live together under evals/retrieval_verdict, which
# is outside the repository test path, so the component root goes on the path
# and the suite is targeted only.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
