# X04's cross-provider parity harness, correlation fixture and results artifact
# live together under evals/provider_parity, which is outside the repository
# test path, so the component root goes on the path and the suite is targeted
# only.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
