"""Epistemic Foundry reasoning surfaces.

Namespace package marker required so the versioned R05 surface under
`src/epistemic_foundry/reasoning/v4_r05/` — R05's declared write scope in
`manifests/development_manifest.yaml` — is discoverable in the built wheel
(proven by `artifacts/work_packages/R05/attempts/0001/check_packaging.py`).
This parent marker sits one level above that glob and carries no logic.
"""
