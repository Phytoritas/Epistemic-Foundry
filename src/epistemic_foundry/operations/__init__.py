"""Production operations and scale surfaces for the v4 Foundry.

This package holds the operations-layer integration surfaces that compose the
already-sealed evaluation, scheduling, archive, budget and provider modules into
the guarantees a production run at scale must hold: quality-diversity coverage,
surrogate triage that never promotes, bounded budgets, and honest load shedding.
"""

from __future__ import annotations
