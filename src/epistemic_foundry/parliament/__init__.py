"""Evidence Parliament promotion surfaces.

This package holds the version-scoped Parliament gates that convene the
multi-dimensional promotion review.  The marker exists so the P-phase packages
are discoverable in an installed distribution, not only in a checkout; the
Parliament recommends and convenes but never holds promotion authority, which
lives in :mod:`epistemic_foundry.governance.promotion`.
"""

from __future__ import annotations
