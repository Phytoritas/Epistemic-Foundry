interface:
  display_name: "Replay"
  short_description: "Replay a run, compare strict or semantic outputs, and propagate staleness without overwriting prior artifacts."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
