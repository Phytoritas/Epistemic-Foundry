interface:
  display_name: Shinka Adapter
  short_description: Use pinned ShinkaEvolve only as an optional program-search backend
    behind Foundry authority.
policy:
  allow_implicit_invocation: false
