interface:
  display_name: "Shinka Adapter"
  short_description: "Use pinned ShinkaEvolve only as an optional program-search backend behind Foundry authority."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
