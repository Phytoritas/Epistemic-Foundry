interface:
  display_name: "Claim Forge"
  short_description: "Extract atomic source-span ClaimCards with scope, method, stance, quantitative context, and provenance."
policy:
  invocation_disposition: IMPLICIT_SAFE
  allow_implicit_invocation: true
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
