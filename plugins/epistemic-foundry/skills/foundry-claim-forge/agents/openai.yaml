interface:
  display_name: Claim Forge
  short_description: Extract atomic source-span ClaimCards with scope, method, stance,
    quantitative context, and provenance.
policy:
  allow_implicit_invocation: true
