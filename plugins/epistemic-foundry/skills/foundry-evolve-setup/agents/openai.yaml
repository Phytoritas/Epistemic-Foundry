interface:
  display_name: Evolve Setup
  short_description: Create a typed EvolutionRunSpec, seed genomes, evaluator and
    holdout manifests, diversity axes, budgets, and stop rules.
policy:
  allow_implicit_invocation: false
