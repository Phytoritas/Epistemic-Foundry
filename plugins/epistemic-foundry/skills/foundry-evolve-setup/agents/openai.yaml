interface:
  display_name: "Evolve Setup"
  short_description: "Create a typed EvolutionRunSpec, seed genomes, evaluator and holdout manifests, diversity axes, budgets, and stop rules."
policy:
  invocation_disposition: PARENT_ROUTED
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
