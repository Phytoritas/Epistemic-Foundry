interface:
  display_name: "Domain Pack"
  short_description: "Author a versioned DomainPack for ontology, measurement, methods, evidence hierarchy, coverage, and contradiction rules."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
