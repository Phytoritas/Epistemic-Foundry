interface:
  display_name: "Passport"
  short_description: "Build or inspect a provenance-bound Hypothesis Passport after evidence and gate artifacts exist."
policy:
  invocation_disposition: IMPLICIT_SAFE
  allow_implicit_invocation: true
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
