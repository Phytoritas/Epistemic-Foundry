interface:
  display_name: "Admin"
  short_description: "Explicit-only administration for health, policy, consent, backup, migration, rollback, signing, and recovery."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
