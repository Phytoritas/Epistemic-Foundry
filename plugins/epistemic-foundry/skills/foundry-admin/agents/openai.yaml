interface:
  display_name: Admin
  short_description: Explicit-only administration for health, policy, consent, backup,
    migration, rollback, signing, and recovery.
policy:
  allow_implicit_invocation: false
