interface:
  display_name: "Atlas"
  short_description: "Map evidence coverage, contradictions, dependencies, method compatibility, and searched or unsearched gaps."
policy:
  invocation_disposition: IMPLICIT_SAFE
  allow_implicit_invocation: true
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
