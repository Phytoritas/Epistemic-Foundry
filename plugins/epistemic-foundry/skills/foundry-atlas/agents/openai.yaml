interface:
  display_name: Atlas
  short_description: Map evidence coverage, contradictions, dependencies, method compatibility,
    and searched or unsearched gaps.
policy:
  allow_implicit_invocation: true
