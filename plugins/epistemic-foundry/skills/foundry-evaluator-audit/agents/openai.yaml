interface:
  display_name: "Evaluator Audit"
  short_description: "Qualify an evaluator bundle as a fallible scientific instrument before scoring or after a defect proposal."
policy:
  invocation_disposition: PARENT_ROUTED
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
