interface:
  display_name: Evaluator Audit
  short_description: Qualify an evaluator bundle as a fallible scientific instrument
    before scoring or after a defect proposal.
policy:
  allow_implicit_invocation: false
