interface:
  display_name: Observe
  short_description: Acquire relation-aware evidence with receipts across support,
    counter, null, boundary, method, temporal, and novelty lanes.
policy:
  allow_implicit_invocation: true
