interface:
  display_name: "Observe"
  short_description: "Acquire relation-aware evidence with receipts across support, counter, null, boundary, method, temporal, and novelty lanes."
policy:
  invocation_disposition: IMPLICIT_SAFE
  allow_implicit_invocation: true
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
