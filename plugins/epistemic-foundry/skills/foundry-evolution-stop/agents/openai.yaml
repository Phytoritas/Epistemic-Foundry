interface:
  display_name: "Evolution Stop"
  short_description: "Stop, pause, or cancel evolution safely with a typed stop certificate and atomic checkpoint."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
