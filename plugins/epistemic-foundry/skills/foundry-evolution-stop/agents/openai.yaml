interface:
  display_name: Evolution Stop
  short_description: Stop, pause, or cancel evolution safely with a typed stop certificate
    and atomic checkpoint.
policy:
  allow_implicit_invocation: false
