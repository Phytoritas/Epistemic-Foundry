interface:
  display_name: Validation
  short_description: Preregister and execute validation under capability leases and
    receipts without relabeling computation as empirical evidence.
policy:
  allow_implicit_invocation: false
