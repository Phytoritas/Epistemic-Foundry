interface:
  display_name: "Validation"
  short_description: "Preregister and execute validation under capability leases and receipts without relabeling computation as empirical evidence."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
