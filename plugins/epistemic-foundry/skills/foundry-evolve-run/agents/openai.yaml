interface:
  display_name: Evolve Run
  short_description: Run or resume governed evolution with bounded workers, immutable
    evaluators, receipts, checkpoints, and explicit stop conditions.
policy:
  allow_implicit_invocation: false
