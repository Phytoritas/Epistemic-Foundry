interface:
  display_name: "Evolve Run"
  short_description: "Run or resume governed evolution with bounded workers, immutable evaluators, receipts, checkpoints, and explicit stop conditions."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
