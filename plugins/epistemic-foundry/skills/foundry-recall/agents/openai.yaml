interface:
  display_name: "Recall"
  short_description: "Retrieve prior decisions only when needed and permitted; memory is not source evidence."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
