interface:
  display_name: "Evolution Replay"
  short_description: "Replay an evolution cycle or lineage from a checkpoint under strict, semantic, or provider-nondeterministic equivalence."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
