interface:
  display_name: Archive
  short_description: Inspect or rebalance the quality-diversity archive while preserving
    negative, unsafe, failed, and minority lineages.
policy:
  allow_implicit_invocation: false
