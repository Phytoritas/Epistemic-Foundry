interface:
  display_name: "Archive"
  short_description: "Inspect or rebalance the quality-diversity archive while preserving negative, unsafe, failed, and minority lineages."
policy:
  invocation_disposition: PARENT_ROUTED
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
