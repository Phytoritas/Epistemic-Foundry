interface:
  display_name: "Plugin Dev"
  short_description: "Build, test, audit, package, migrate, or release Epistemic Foundry without self-approval or unsupported readiness claims."
policy:
  invocation_disposition: EXPLICIT_ONLY
  allow_implicit_invocation: false
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
