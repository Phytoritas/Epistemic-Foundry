interface:
  display_name: "Map"
  short_description: "Map code, schemas, workflows, papers, data, claims, evidence, artifacts, tests, and authority dependencies."
policy:
  invocation_disposition: IMPLICIT_SAFE
  allow_implicit_invocation: true
  sensitive: false
  side_effecting: false
  load_full_instructions: on_demand
