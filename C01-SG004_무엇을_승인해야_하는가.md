# C01-SG004는 무엇이고, 무엇을 승인해야 하는가

## 한 문장으로 말하면

**새 문서 등록 계약 2개를 추가하여 canonical schema와 example을 각각 124개에서 126개로 늘리는 것은 이미 승인됐지만, 기존 “정답표”와 테스트 8곳은 아직 124개를 정답으로 강제하고 있고, 그 파일을 누가 언제 126개로 바꿀지에 대한 권한이 빠져 있어 작업이 멈춘 상태입니다.**

이것은 새 기능의 의미가 불명확해서 생긴 문제가 아닙니다. 구현 도구나 외부 자원이 없어서 생긴 문제도 아닙니다. **상위 명세·테스트·패키지 투영을 함께 바꿀 정확한 권한과 실행 순서가 빠진 문제**입니다.

---

## 비유로 이해하기

현재 상황을 창고 재고표에 비유하면 다음과 같습니다.

- 새 물품 2종을 창고에 넣는 결정은 이미 내려졌습니다.
- 따라서 실제 물품 종류는 `124 → 126`이 되어야 합니다.
- 그런데 본사 재고표와 검사 체크리스트는 여전히 “정답은 124종”이라고 적혀 있습니다.
- 창고 프로그램에 126종을 넣으면 검사기는 “124종이어야 하는데 126종이므로 실패”라고 판정합니다.
- 반대로 검사를 통과시키려고 124종을 유지하면 이미 승인된 새 물품 2종을 구현할 수 없습니다.

즉, 필요한 것은 기능을 다시 결정하는 일이 아니라 **재고표와 검사 체크리스트도 126종으로 바꾸도록 정확한 담당자와 시점을 승인하는 일**입니다.

---

## 이미 승인된 것

다음 제품 의미는 이미 승인되어 있으므로 다시 결정할 필요가 없습니다.

1. canonical schema 수를 `124 → 126`으로 변경합니다.
2. canonical example 수도 `124 → 126`으로 변경합니다.
3. 다음 canonical 계약 2개를 추가합니다.
   - `DocumentRegistrationRequest`
   - `DocumentRegistration`
4. C01이 새 schema, example, OpenAPI 및 API 문서를 구현합니다.
5. 이후 기본 흐름은 `C01 → C02 → C03 → C04 → B04`입니다.
6. 기존 C01 attempt와 RAH 이력은 삭제하거나 소급 변경하지 않습니다.

## 아직 승인되지 않은 것

다음 사항이 명시적으로 정해지지 않았습니다.

1. 124를 정답으로 고정한 상위 명세와 acceptance matrix를 누가 126으로 바꿀지
2. C01의 새 계약과 충돌하는 OpenAPI 테스트를 누가 바꿀지
3. runtime registry와 CLI가 126개를 기대하도록 누가 바꿀지
4. F01의 schema/example 개수 테스트를 C04 전에 누가 교정할지
5. B04가 소유한 package snapshot과 packaging test를 C04 전체 테스트 전에 어떻게 재투영할지
6. 이 교정을 위해 허용되는 정확한 파일 범위와 실행 시점

이 공백의 식별자가 `C01-SG004`입니다.

---

## 충돌하는 8개 파일

| 파일 | 현재 상태 | 필요한 최소 교정 | 권장 담당/시점 |
|---|---|---|---|
| `MASTER_SPEC.md` | 124 schemas / 124 examples를 최상위 정답으로 선언 | 126/126으로 교정하고 새 schema 2개를 목록에 반영 | 제품소유자 권한의 pre-C01 shared-contract correction |
| `manifests/acceptance_matrix.yaml` | `canonical_schema_count: 124`, `matching_example_count: 124` | 둘 다 126으로 교정 | 제품소유자 권한의 pre-C01 shared-contract correction |
| `tests/contracts/openapi/test_scientific_contracts.py` | schema/example 수를 124로 고정 | 126/126 및 1:1 mapping을 검증 | C01 새 attempt, C01 검증 전에 교정 |
| `tests/contracts/openapi/test_openapi_contract.py` | 수를 124로 고정하고 `DocumentRegistrationRequest`를 transport-only type으로 취급 | canonical schema `$ref`와 새 request/registration 의미를 검증 | C01 새 attempt, C01 검증 전에 교정 |
| `tests/test_contracts.py` | registry schema 수를 124로 고정 | fresh package projection에서 126을 검증 | B04 pre-C04 correction과 함께 교정 |
| `tests/test_cli.py` | CLI의 loaded schema 수를 124로 고정 | fresh package projection에서 126을 검증 | B04 pre-C04 correction과 함께 교정 |
| `tests/test_f01_epistemic_work_classifier.py` | root schema/example 수를 124/124로 고정 | 126/126으로 교정하되 classifier 의미는 변경하지 않음 | 제품소유자가 승인한 count-only correction, C04 전에 실행 |
| `tests/packaging/test_canonical_registry.py` | packaged schema 124개와 총 resource 125개를 고정 | schema 126개, OpenAPI 포함 총 resource 127개 및 새 hash/receipt 검증 | B04 pre-C04 correction |

중요한 점은 `tests/**` 전체 수정 권한이 필요한 것이 아니라는 것입니다. **위 8개 exact path에 대한 제한적 교정 권한만 있으면 됩니다.**

---

## 왜 현재 실행 순서로는 C04를 통과할 수 없는가

현재 정적 순서는 다음과 같습니다.

```text
C01: root schema/example을 126개로 변경
  ↓
C02: generated models를 126개 기준으로 재생성
  ↓
C03: runtime migration 정합화
  ↓
C04: 전체 Python suite가 0 failures인지 검사
  ↓
B04: package snapshot을 root와 맞게 재투영
```

문제는 C04가 B04보다 먼저 실행된다는 데 있습니다.

```text
root canonical source        = 126개
기존 packaged snapshot       = 124개
기존 packaging/registry test = 124개가 정답

C04 full_python_suite 실행
→ root와 package snapshot 불일치
→ 126을 구현해도 실패
→ B04는 C04 뒤라서 아직 고칠 수 없음
```

따라서 정적 package dependency를 뒤집어 순환 의존성을 만들 필요는 없지만, **C04 전에 B04가 한 번 재투영하는 attempt-level reconciliation**은 명시적으로 승인해야 합니다.

권장 흐름은 다음과 같습니다.

```text
상위 정답표 126 교정
  ↓
C01 새 attempt
  ↓
C02
  ↓
C03
  ↓
B04 pre-C04 correction
  - package snapshot 126으로 재투영
  - registry/CLI/packaging oracle 교정
  - projection receipt 생성
  ↓
C04 full_python_suite 및 전체 conformance gate
  ↓
B04 정상 final packaging gate
```

pre-C04 B04 correction은 정적 DAG의 영구 dependency를 바꾸는 것이 아닙니다. C04가 검사할 입력을 최신 상태로 만드는 **이번 migration의 제한적 복구 단계**입니다.

---

## 사용자가 실제로 승인해야 하는 최소 내용

다음 여섯 가지만 명확히 승인하면 `C01-SG004`를 해소할 수 있습니다.

1. canonical schema/example의 최종 정답은 각각 126개입니다.
2. 위 8개 exact path만 126 전환을 위해 교정할 수 있습니다.
3. `MASTER_SPEC.md`와 `acceptance_matrix.yaml`의 124를 126으로 바꿀 수 있습니다.
4. C01의 OpenAPI contract test 2개는 C01 검증 전에 교정합니다.
5. B04 snapshot 재투영과 registry/CLI/packaging oracle 교정은 C04 full suite 전에 attempt-level reconciliation으로 실행합니다.
6. 기존 C01-0001~C01-0005, 기존 report/review/RAH evidence 및 dirty worktree는 그대로 보존합니다.

승인하지 않아도 되는 것은 다음과 같습니다.

- broad `tests/**`, `docs/**`, `schemas/**` 수정 권한
- schema 제약 완화
- 테스트 skip/xfail
- generated output 수동 편집
- package snapshot을 canonical source로 승격
- 기존 실패 이력 삭제
- 정적 DAG에 `C04 ↔ B04` 순환 dependency 추가

---

## 현재 상태

`C01-0005`는 구현 실패가 아니라, 잘못된 권한으로 진행하지 않기 위해 멈춘 contract audit입니다.

- 상태: `SPEC_GAP`
- blocker: `C01-SG004`
- 제품 파일 변경: 0
- 기존 OpenAPI contract baseline: 66 passed
- C02 이후 작업: 시작하지 않음
- 기존 C01-0001~C01-0004: 보존됨
- RAH 상태: `blocked`
- `completion_ready`: `false`

따라서 새 결정이 durable하게 기록되면 기존 attempt를 수정하는 것이 아니라 **새 C01 attempt**로 재개해야 합니다.

---

## 그대로 복사해 사용할 수 있는 HumanDecision 초안

아래 블록은 `C01-SG004`를 해소하기 위한 권장 최소 승인문입니다. 이 문서에 초안이 저장된 것 자체는 승인 기록이 아니며, 제품소유자가 이 내용을 명시적으로 승인하고 durable HumanDecision으로 기록해야 효력이 생깁니다.

```text
# AUTHORITATIVE PRODUCT-OWNER SHARED-CONTRACT DECISION
# Epistemic Foundry v4.0 — C01-SG004
# Decision ID: HD-EF4-C01-SG004-20260730-001

이 메시지는 사용자이자 제품 최상위 권한자의 명시적
shared-contract HumanDecision이다.

C01-0005의 SPEC_GAP 판정과 fail-closed 중단은 정당하다.
C01-0005를 PASS로 소급 변경하지 않는다.
기존 C01-0001부터 C01-0005까지의 report, review, commands,
RAH evidence, generation 및 기존 dirty working tree를 보존한다.

1. 최종 canonical count

- canonical JSON Schema count: 126
- matching canonical example count: 126
- 새 canonical contracts:
  - DocumentRegistrationRequest
  - DocumentRegistration

기존 124 count는 이 migration 이전 이력에만 유효하다.
active authority와 active acceptance oracle은 126으로 교정한다.

2. exact-path correction authority

이번 결정으로 다음 8개 exact path만 shared-contract/oracle
correction 대상으로 승인한다.

- MASTER_SPEC.md
- manifests/acceptance_matrix.yaml
- tests/contracts/openapi/test_scientific_contracts.py
- tests/contracts/openapi/test_openapi_contract.py
- tests/test_contracts.py
- tests/test_cli.py
- tests/test_f01_epistemic_work_classifier.py
- tests/packaging/test_canonical_registry.py

broad tests/**, docs/**, schemas/** 또는 manifests/** 권한은
부여하지 않는다.

3. correction ownership과 timing

3.1 Pre-C01 shared-contract correction

primary session은 이 HumanDecision에 근거하여 C01 시작 전에
다음 exact correction을 수행할 수 있다.

- MASTER_SPEC.md:
  - active canonical schema/example count를 126/126으로 교정
  - DocumentRegistrationRequest와 DocumentRegistration을
    canonical schema inventory에 반영
- manifests/acceptance_matrix.yaml:
  - canonical_schema_count를 126으로 교정
  - matching_example_count를 126으로 교정
- tests/test_f01_epistemic_work_classifier.py:
  - root canonical schema/example count assertion만 126/126으로 교정
  - classifier behavior, gold answer 또는 protection gate는 변경하지 않음

이는 A01 또는 F01의 과거 PASS를 재작성하지 않는 bounded
shared-contract correction이다.

3.2 C01 correction

새 C01 attempt는 기존 C01 scope에 더해 다음 두 exact test path를
수정할 수 있다.

- tests/contracts/openapi/test_scientific_contracts.py
- tests/contracts/openapi/test_openapi_contract.py

C01은 다음을 검증한다.

- 126 schemas / 126 examples
- one-to-one schema/example mapping
- DocumentRegistrationRequest가 transport-only duplicate가 아니라
  canonical JSON Schema reference임
- DocumentRegistration canonical response mapping
- 승인된 request/registration semantic contract

C01 targeted validation 전에 위 oracle correction을 완료한다.

3.3 Pre-C04 B04 attempt-level reconciliation

C01, C02, C03가 완료된 뒤 C04 full_python_suite 전에
B04가 re-entrant correction attempt를 수행하는 것을 승인한다.

B04 correction의 추가 exact write scope는 다음으로 제한한다.

- tests/test_contracts.py
- tests/test_cli.py

기존 B04 scope의 다음 경로는 그대로 사용한다.

- src/epistemic_foundry/_canonical/**
- scripts/build/canonical_registry/**
- tests/packaging/test_canonical_registry.py
- artifacts/work_packages/B04/**

B04 correction은 다음을 수행한다.

- root schemas/**와 openapi/**를 sole authority로 사용
- package snapshot을 현재 126-schema root bundle에서 재투영
- canonical-registry.json을 deterministic하게 재생성
- tests/test_contracts.py의 expected schema count를 126으로 교정
- tests/test_cli.py의 loaded schema count를 126으로 교정
- packaging test를 schema 126, OpenAPI 1,
  total canonical resources 127 기준으로 교정
- source/snapshot path, count, byte hash parity 검증
- installed-wheel-only resource load 검증
- machine-readable projection receipt 생성

이 pre-C04 correction은 attempt-level reconciliation이며
B04와 C04 사이에 영구 정적 dependency cycle을 만들지 않는다.
C04 PASS 후 기존 계약에 따른 B04 final packaging gate를 실행한다.

4. fixed resume sequence

다음 순서로 재개한다.

1. HD-EF4-C01-SG004-20260730-001 durable 기록
2. exact-path manifest/write-scope correction
3. pre-C01 shared-contract correction
4. 새 C01 attempt 및 targeted contract validation
5. C01 PASS 확인
6. C02 실행 및 PASS 확인
7. C03 실행 및 PASS 확인
8. pre-C04 B04 attempt-level projection reconciliation
9. matching projection receipt 확인
10. C04 full_python_suite 및 full conformance gate
11. C04 PASS 확인
12. B04 final packaging gate
13. B04 PASS 확인
14. 전체 dependency DAG 재계산
15. 다음 dependency-ready package 재개

5. preservation and prohibitions

다음을 계속 금지한다.

- 기존 C01 attempt/report/evidence 삭제 또는 소급 PASS 처리
- schema weakening
- 124와 126을 동시에 active truth로 유지
- skip, xfail 또는 failure allowlist로 충돌 은폐
- generated C02 output 수동 편집
- package snapshot을 canonical source로 취급
- root source를 stale snapshot에 맞추는 reverse synchronization
- broad tests/**, schemas/**, docs/** 권한 확대
- 정적 C04↔B04 dependency cycle 추가
- receipt 없는 projection/completion
- reset, clean 또는 dirty working tree 훼손
- completion_ready 조기 true 전환

6. state transition

이 HumanDecision이 durable하게 기록된 뒤:

- C01-SG004: RESOLVED_BY_DECISION
- C01-0005: immutable SPEC_GAP history
- C01: READY_FOR_NEW_ATTEMPT
- C02: WAITING_ON_C01
- C03: WAITING_ON_C01_AND_C02
- C04: WAITING_ON_C02_C03_AND_FRESH_PROJECTION
- B04 pre-C04 correction: AUTHORIZED_WHEN_C01_C02_C03_COMPLETE
- completion_ready: false

추가 확인 질문 없이 위 exact scope와 순서에 따라 primary session에서
재개하라. 기존 이력과 dirty working tree를 보존하라.
```

---

## 결론

사용자에게 필요한 결정은 “새 schema 2개를 만들 것인가?”가 아닙니다. 그것은 이미 승인됐습니다.

필요한 결정은 다음 한 문장입니다.

> **124에서 126으로 바뀌는 동안 함께 틀어지는 상위 정답표와 테스트 8곳을 정확히 누가 언제 고칠지, 그리고 B04 재투영을 C04 전체 검사 전에 한 번 수행해도 되는지를 승인한다.**

이 결정이 durable하게 기록되면 C01은 새 attempt에서 구현을 다시 시작할 수 있습니다.
