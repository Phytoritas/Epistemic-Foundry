# Claude Code adapter

The production adapter generates `.claude/agents` and skill files from the same RoleSpec registry. The main session remains the research governor. Read-only roles use restricted tools; write-capable roles require isolated worktrees and disjoint write scopes. Results return through the same ResultEnvelope and ledger as Codex.

Host-specific convenience never changes FORGE transition or epistemic promotion semantics.
