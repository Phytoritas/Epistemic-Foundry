# HumanDecision HD-EF4-J02-SG001-20260729-001

Status: `CANONICAL PRODUCT-OWNER DECISION`

This decision resolves `J02-SG001` prospectively and authorizes only the new
attempt `J02-0002`. It preserves `J02-0001`, its `SPEC_GAP`, all earlier
reports, command records, reviews, RAH evidence and generations, J01 routing
results, J03's unstarted state, the 156-package DAG, and the existing dirty
worktree. It does not make the external goal completion-ready.

## Decision metadata

```yaml
decision_id: HD-EF4-J02-SG001-20260729-001
subject_id: J02-SG001
decision_type: correct
authority_role: product_owner
authorized_attempt: J02-0002
preserve_history: true
preserve_dirty_worktree: true
fleet_forbidden: true
subagents_forbidden: true
completion_ready: false
```

The canonical JSON record contains only fields admitted by
`schemas/human-decision.schema.json`. The complete product contract and exact
acceptance oracle are retained in `docs/skill_context_contract.md`.

## Fixed product decisions

- Production discovery contains exactly 29 active skills: parent `foundry`
  plus 28 child skills. The blueprint is migration input, never runtime
  authority.
- Initial metadata contains only normalized `name`, `description`, and
  relative `SKILL.md` path. Its hard limits are 6,400 UTF-8 bytes and 1,600
  `tiktoken==0.13.0` `o200k_base` tokens, both enforced.
- A loaded `SKILL.md` is limited to 4,096 bytes/1,024 tokens. Each atomic
  Markdown reference has the same limit. A closure is limited to 12 files,
  depth 5, 24,576 bytes and 6,144 tokens; total activation is limited to
  28,672 bytes and 7,168 tokens.
- The canonical source of skill/reference inventory is
  `plugins/epistemic-foundry/skills/skill-inventory.json`. It binds all file
  paths, hashes, byte counts, token counts, invocation dispositions,
  reference dependencies, direct mappings, predicates, and authority sources.
- The reference graph contains the exact 17 `EFREF-*` records and exact
  skill-to-reference mapping fixed by the product owner. Selection is typed,
  deterministic, deduplicated by canonical reference ID, and topologically
  ordered with stable priority and UTF-8 byte ordering.
- Missing nodes, cycles, depth/count/budget overflow, content drift, unknown
  proposals, disabled references, traversal, symlinks, junctions, hard-link
  escapes, or authority drift fail closed. There is no partial load,
  truncation, semantic-similarity fallback, or model-authored repair.
- J01 owns skill routing; J02 owns metadata accounting, reference resolution,
  reachability and ordered load under `packages/plugin-host/src/skill-context`;
  J03 owns ContextCapsule assembly. J02 and J03 remain independent children of
  J01, and only J04 depends on both.
- Canonical token accounting uses `tiktoken==0.13.0`, encoding
  `o200k_base`, and `disallowed_special=()`. Missing or different tokenizer
  state fails `TOKENIZER_CONTRACT_UNAVAILABLE`; approximation is forbidden.

## Invocation dispositions

- `PARENT_ROUTER`: `foundry`.
- `IMPLICIT_SAFE`: `foundry-intake`, `foundry-claim-forge`,
  `foundry-observe`, `foundry-atlas`, `foundry-reason`, `foundry-aporia`,
  `foundry-map`, `foundry-passport`, `foundry-evolve-inspect`.
- `PARENT_ROUTED`: `foundry-parliament`, `foundry-evolve`,
  `foundry-evolve-setup`, `foundry-evaluator-audit`, `foundry-challenge`,
  `foundry-archive`.
- `EXPLICIT_ONLY`: the remaining 13 child skills named in the PRD.

Activation never grants mutation, approval, promotion, holdout, evaluator, or
policy authority. `agents/openai.yaml` is a verified projection of inventory,
not a second authority.

## Acceptance and history relation

`context_budget_test` must pass all metadata, tokenizer, progressive-load and
12 boundary oracles exactly. `reference_reachability_test` must prove 29/29
skill nodes, 17/17 references, 35/35 selection cases, 16/16 adversarial
fail-closed cases, and 100/100 deterministic replays. J01 routing and metadata
lint must remain unchanged, full Python/Node regressions must add no failure or
skip/xfail, and a procedurally separate primary-session review must record its
non-actor-independent limitation.

```text
J02-SG001
  --resolved_by--> HD-EF4-J02-SG001-20260729-001
HD-EF4-J02-SG001-20260729-001
  --authorizes--> J02-0002
```

The blocker remains immutable history. J04 remains waiting until both J02 and
J03 independently pass.
