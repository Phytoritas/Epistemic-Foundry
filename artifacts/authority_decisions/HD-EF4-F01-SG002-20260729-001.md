# HumanDecision HD-EF4-F01-SG002-20260729-001

Status: `CANONICAL PRODUCT-OWNER SHARED-CONTRACT DECISION`

This decision resolves `F01-SG002` prospectively. It does not relabel
`F01-0002`, delete its blocker, alter the previous `B04-0002` PASS record, or
rewrite any report, command, review, RAH evidence, generation, or dirty
working-tree content. `completion_ready` remains false.

## Decision metadata

```yaml
decision_id: HD-EF4-F01-SG002-20260729-001
subject_id: F01-SG002
decision_type: correct
authority_role: product_owner
parent_decision_id: HD-EF4-F01-SG001-20260729-001
canonical_source_authority:
  - schemas/**
  - openapi/**
projection_owner: B04
projection_target: src/epistemic_foundry/_canonical/**
correction_execution: next unused B04 attempt
f01_waits_for_projection: true
f02_f03_wait_for_f01: true
static_dependency_cycle_forbidden: true
attempt_level_reconciliation_required: true
preserve_history: true
preserve_dirty_worktree: true
fleet_forbidden: true
subagents_forbidden: true
completion_ready: false
```

The canonical JSON record uses only fields admitted by
`schemas/human-decision.schema.json`. This companion record retains the full
shared-contract meaning.

## Authority and projection contract

Only the root `schemas/**` and `openapi/**` trees define canonical meaning.
`src/epistemic_foundry/_canonical/**` is a deterministic package-resource
snapshot for wheel, sdist, and installed-runtime access. It is not a second
authority. Projection is one-way from root source to the package snapshot.
Reverse synchronization, first-found authority, repository-root fallback,
editable-install-only fallback, and independent root/snapshot semantic edits
are forbidden.

B04 is the re-entrant projection owner. An approved change, addition, or
deletion under the canonical root trees, a root/registry bundle-hash change,
an inventory or byte mismatch, a missing installed resource, or an integrity
failure makes the package snapshot stale. The source-changing package remains
`WAITING_ON_CANONICAL_PROJECTION` until a new B04 correction attempt issues a
matching projection receipt and that package passes a new acceptance attempt.

This correction is represented by attempt provenance, not a permanent static
dependency cycle:

```text
F01-0002
  --detected_projection_drift--> B04-0003
B04-0003
  --produces_matching_projection_receipt--> F01-0003
F01-0003
  --revalidates_against_projection--> F01 package gate
```

The existing package DAG remains unchanged: F02 and F03 each continue to
depend only on F01 and remain waiting until an actual F01 PASS.

## B04 correction boundary

The next unused allocator result is `B04-0003`. The correction may write only
the derived snapshot and B04 attempt evidence:

- `src/epistemic_foundry/_canonical/**`
- `artifacts/work_packages/B04/**`

Root schemas and OpenAPI, `pyproject.toml`, the existing projection/build
scripts, packaging tests, F01 code and tests, package manifests, unrelated
runtime files, and other work-package evidence are read-only for this
correction. If the existing projection mechanism must itself be changed to
meet this decision, B04-0003 must not invent or patch a generator outside that
boundary. A defined but defective mechanism is `FAIL`; unclear ownership is a
new minimal `SPEC_GAP`; a missing required tool or backend is `BLOCKED`.

Before replacement, B04 seals a sorted root inventory and a deterministic
source-bundle hash over normalized root-relative source and target paths,
sizes, and content hashes. It stages the complete snapshot away from the live
target, copies root bytes one way, regenerates the registry, verifies staged
bytes, rechecks that the root bundle did not change, atomically replaces the
target, and verifies the reopened target. A root change during projection is
`SOURCE_CHANGED_DURING_PROJECTION`; a partial copy is never success.

The regenerated registry must identify its format, root source bundle,
projected snapshot bundle, available source identity, deterministic entries,
source and package paths, media types, byte sizes, content hashes, document
identities, counts, and projection tool identity/version. Placeholder hashes,
stale copied values, hidden missing/extra resources, duplicate schema IDs,
absolute local paths, and timestamp-only refreshes are forbidden.

`B04-0003` passes only with exact source/snapshot path-count-byte parity,
duplicate IDs zero, an atomic replacement, OpenAPI 3.1.1 with 33 operations,
all current 124 schemas, reference resolution, clean wheel and sdist builds,
sdist-to-wheel rebuild, installed-wheel-only registry/schema/OpenAPI access,
arbitrary-empty-cwd access, no source fallback, resource tamper rejection,
deterministic reprojection/build evidence, no new Python or Node failure,
write-scope compliance, and a separate primary-session integration review.

## F01 revalidation and S04 debt

Only after B04 correction PASS may `F01-0003` run. It revalidates the existing
classifier implementation against the current root bundle, matching B04
receipt, derived snapshot, installed resources, all fixed classifier suites,
1,023 signal subsets, 58,025 monotonicity comparisons, full Python regression,
and full Node regression. F01 may repair only an F01-owned defect and may not
edit the B04 snapshot or registry.

The pre-existing `S04-TM004` stale-hash Node debt remains owned by S04. Its one
exact unchanged fingerprint may coexist with F01 PASS only when comparison
evidence proves no F01/B04 causal impact, no new Node failure, and no skip or
xfail masking. It is not release readiness and is not transferred to F01.

## Required evidence relation and order

```text
F01-SG002
  --resolved_by--> HD-EF4-F01-SG002-20260729-001
HD-EF4-F01-SG002-20260729-001
  --assigns_projection_owner--> B04
F01-0002
  --requires_correction--> B04-0003
B04-0003
  --unblocks_on_PASS--> F01-0003
```

The fixed order is decision record, limited manifest correction, RAH decision
edge and active/resumable transition, B04-0003, B04 review and PASS, F01-0003,
F01 review and PASS, 156-package DAG recomputation, then F02/F03. B04 failure
halts before F01-0003. F01 failure halts before F02/F03. No receipt permits no
completion claim.
