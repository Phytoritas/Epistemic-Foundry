# HumanDecision HD-EF4-C01-SG002-20260728-001

Status: `CANONICAL PRODUCT-OWNER DECISION`

This decision resolves the absence of higher authority recorded as
`C01-SG002`. It applies prospectively to a new `C01-0003` attempt. It does not
delete, rewrite, relabel, or retroactively pass `C01-0001`, `C01-0002`, their
reports, the conflict reproduction, any RAH evidence or generation, or the
existing dirty worktree.

## Decision metadata

```yaml
decision_id: HD-EF4-C01-SG002-20260728-001
subject_id: C01-SG002
decision_type: correct
authority_role: product_owner
parent_decision_id: HD-EF4-A05-C01-B04-20260727-001
approved_write_scope_additions:
  - examples/sample_evolution-run-spec.json
  - examples/sample_promotion-decision.json
preserve_existing_scope: true
preserve_history: true
schema_weakening_forbidden: true
fleet_forbidden: true
subagents_forbidden: true
```

The canonical JSON record stores this meaning inside fields admitted by
`schemas/human-decision.schema.json`. The companion metadata above is not a
request to extend that schema. `supersedes_decision_id` remains `null` because
the parent decision is still valid and is not being superseded.

## Authorized correction

Append the two exact paths above to `C01.write_scope` in
`manifests/development_manifest.yaml`, preserving every existing entry. No
other manifest field changes under this decision. In particular, this does
not authorize `examples/**`, a broader `schemas/**` or `openapi/**` expansion,
runtime/Python implementation, B04 packaging, another package's artifacts, or
schema relaxation.

`sample_evolution-run-spec.json` must implement the complete A05 resolved-pin
inventory with deterministic non-production fixture values and a recomputed
canonical `spec_hash`. `sample_promotion-decision.json` must use
`requested_level: REPLICATED`, `granted_level: EMPIRICALLY_TESTED`, and
`decision: CONDITIONAL`, preserve the partial-replication meaning, and carry a
recomputed canonical `decision_hash`. Legacy aliases and fabricated evidence
are forbidden.

## History and RAH relation

The immutable provenance relation is:

```text
C01-SG002
  --resolved_by--> HD-EF4-C01-SG002-20260728-001
  --authorizes--> C01-0003
```

The historical blocker remains in the ledger. A new evidence entry records
the relation; it does not delete the blocker. C01 is resumable after this
decision but is not PASS until C01-0003 implementation, validation, review,
reporting, receipt evidence, and generation checks all pass. B04 remains
gated on C01 PASS.
