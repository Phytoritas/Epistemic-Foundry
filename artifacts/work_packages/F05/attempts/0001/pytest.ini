# F05's state machine ships under src/epistemic_foundry/evolution/v4_f05, and
# its tests live in this attempt directory because the manifest write scope
# covers the machine and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
