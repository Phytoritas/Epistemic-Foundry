# L06's integration gate ships under src/epistemic_foundry/memory/v4_l06, and
# its tests live in this attempt directory because the manifest write scope
# covers the engine and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
