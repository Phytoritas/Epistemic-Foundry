# I06's intake integration gate ships under src/epistemic_foundry/intake/v4_i06,
# and its tests live in this attempt directory because the manifest write scope
# covers the gate and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
