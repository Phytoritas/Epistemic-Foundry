# W05's workflow ships under src/epistemic_foundry/recovery/v4_w05, and its
# tests live in this attempt directory because the manifest write scope covers
# the workflow package and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
