# O06's search-completeness / novelty-failure / prior-art integration gate ships
# under src/epistemic_foundry/retrieval/v4_o06, and its tests live in this attempt
# directory because the manifest write scope covers that package and this attempt
# only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
