# Y05's quality-diversity scaling, surrogate triage, budget and load-shedding
# surface ships under src/epistemic_foundry/operations/v4_y05, and its tests live
# in this attempt directory because the manifest write scope covers the surface
# and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
