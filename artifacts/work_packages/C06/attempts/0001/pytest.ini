# C06's product tests live beside the gate inside the attempt directory (the
# manifest write scope is schemas/v4_c06/** plus this attempt), so the attempt
# root goes on the path and only test_*.py modules are collected.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
