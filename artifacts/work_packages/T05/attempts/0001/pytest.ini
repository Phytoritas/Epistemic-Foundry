# T05's adapter surface ships under src/epistemic_foundry/adapters/v4_t05, and
# its tests live in this attempt directory because the manifest write scope
# covers the adapter and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
