# Y06's 2,000-document qualification and cost/latency integration gate ships under
# src/epistemic_foundry/operations/v4_y06, and its tests live in this attempt
# directory because the manifest write scope covers the surface and this package
# only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
