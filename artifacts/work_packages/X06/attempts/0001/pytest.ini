# X06's provider-diversity integration gate ships under
# src/epistemic_foundry/providers/v4_x06, and its tests live in this attempt
# directory because the manifest write scope covers the surface and this package
# only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
