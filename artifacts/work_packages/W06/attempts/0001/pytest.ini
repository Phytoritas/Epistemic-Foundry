# W06's crash-recovery, replay and reconciliation integration gate ships under
# src/epistemic_foundry/recovery/v4_w06, and its tests live in this attempt
# directory because the manifest write scope covers that package and this
# attempt only. Both roots go on the path: this directory for `fixtures`, and the
# source tree for `epistemic_foundry`.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
