# J06's integration gate ships under src/epistemic_foundry/operators/v4_j06,
# and its tests live in this attempt directory because the manifest write scope
# covers the operator surface and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
