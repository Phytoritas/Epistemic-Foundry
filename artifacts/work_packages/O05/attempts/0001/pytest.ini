# O05's evolution retrieval, layered novelty and coverage-debt acquisition ship
# under src/epistemic_foundry/retrieval/v4_o05, and their tests live in this
# attempt directory because the manifest write scope covers that package and
# this attempt only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
