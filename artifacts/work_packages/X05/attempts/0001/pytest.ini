# X05's cross-provider routing and safe-bandit surface ships under
# src/epistemic_foundry/providers/v4_x05, and its tests live in this attempt
# directory because the manifest write scope covers the surface and this package
# only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
