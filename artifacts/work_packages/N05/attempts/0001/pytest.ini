# N05's lane scheduler ships under src/epistemic_foundry/scheduler/v4_n05, and
# its tests live in this attempt directory because the manifest write scope
# covers the scheduler and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
