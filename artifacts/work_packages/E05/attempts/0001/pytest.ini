# E05's engine ships under src/epistemic_foundry/effects/v4_e05, and its tests
# live in this attempt directory because the manifest write scope covers the
# engine and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
