# K05's corpus/holdout/prior-art boundaries ship under
# src/epistemic_foundry/evidence/v4_k05, and their tests live in this attempt
# directory because the manifest write scope covers the boundaries and this
# package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
