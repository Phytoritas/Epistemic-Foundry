# J05's operator registry ships under src/epistemic_foundry/operators/v4_j05,
# and its tests live in this attempt directory because the manifest write scope
# covers the engine and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
