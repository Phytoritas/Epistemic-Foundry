# N06's integration gate ships under src/epistemic_foundry/scheduler/v4_n06, and
# its tests live in this attempt directory because the manifest write scope
# covers that package and this attempt only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
