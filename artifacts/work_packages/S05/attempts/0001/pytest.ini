# S05's threat controls ship under src/epistemic_foundry/security/v4_s05, and
# their tests live in this attempt directory because the manifest write scope
# covers the controls and this package only. Both roots go on the path.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
