# B06's gate module and its tests both live in this attempt directory (the
# manifest write scope is build/v4_b06/** plus this attempt), so the attempt
# root goes on the path.  The package source root joins it because the
# provenance suite cross-checks against the shipped canonical contracts.
[pytest]
testpaths = .
addopts = -q
pythonpath = . ../../../../../src
