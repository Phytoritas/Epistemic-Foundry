# D05's migration and its tests live in the attempt directory (the manifest
# write scope is migrations/v4_d05/** plus this attempt), so the attempt root
# goes on the path.  The suite drives a real PostgreSQL container; it is
# targeted only and never collected by the repository test path.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
