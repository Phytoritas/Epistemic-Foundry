# B05's product tests live beside the generator inside the attempt directory
# (the manifest write scope is build/v4_b05/** plus this attempt), so the
# attempt root goes on the path and only test_*.py modules are collected.
[pytest]
testpaths = .
addopts = -q
pythonpath = .
